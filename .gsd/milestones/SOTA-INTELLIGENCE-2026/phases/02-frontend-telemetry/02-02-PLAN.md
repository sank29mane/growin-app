---
phase: 02-frontend-telemetry
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [Growin/ViewModels/GoalPlannerViewModel.swift, Growin/Views/GoalPlannerView.swift]
autonomous: true
requirements: [FE-05]
---

<objective>
Overhaul the Goal Planner Screen based on the Stitch UI Design generated. Modernize the view with a dark theme, deep charcoals, glassmorphism, and premium typography. Migrate the GoalPlannerViewModel to use the modern `@Observable` macro for superior state management.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@Growin/ThemeComponents.swift
@Growin/Color+Extension.swift
@Growin/ViewModels/GoalPlannerViewModel.swift
@Growin/Views/GoalPlannerView.swift
</context>

<tasks>

<task type="auto">
  <name>Migrate GoalPlannerViewModel to @Observable</name>
  <files>Growin/ViewModels/GoalPlannerViewModel.swift</files>
  <action>
    - Refactor `GoalPlannerViewModel` from `ObservableObject` to the `@Observable` macro.
    - Annotate the class with `@MainActor` to ensure thread safety.
    - Remove `@Published` property wrappers, as `@Observable` tracks properties by default.
    - Ensure clear data flow: make internal state `private(set)` if it should only be modified via intents/methods.
  </action>
  <verify>
    Ensure `GoalPlannerViewModel` compiles successfully without `ObservableObject` and `@Published`.
  </verify>
  <done>
    `GoalPlannerViewModel` fully utilizes the modern `@Observable` framework.
  </done>
</task>

<task type="auto">
  <name>Overhaul GoalPlannerView with Stitch Design</name>
  <files>Growin/Views/GoalPlannerView.swift</files>
  <action>
    - Overhaul the `GoalPlannerView` using `ThemeComponents` (`GlassCard`, `MeshBackground`) and the newly updated `Color` extensions (e.g., `stitchNeonGreen`, `stitchNeonIndigo`).
    - Remove `@StateObject` or `@ObservedObject` usage for the `GoalPlannerViewModel` and replace it with `@State` (if instantiated locally) or direct property access.
    - Enhance Data Visualizations: Implement segmented bars or elegant concentric circles for goal progress tracking.
    - Apply premium typography modifiers.
    - Introduce the "AI Strategy Hub" sidebar styling with micro-shadows and subtle glassmorphic blurs to match the Stitch output.
    - Add an interactive visual feedback slider for the "Scenario Simulator".
    - Ensure responsive design padding/margins for macOS and iOS targets.
  </action>
  <verify>
    Ensure `GoalPlannerView` builds correctly and references `ThemeComponents` appropriately.
  </verify>
  <done>
    `GoalPlannerView` perfectly reflects the ultra-high-end Stitch pro trading terminal aesthetic.
  </done>
</task>

</tasks>

<must_haves>
  truths:
    - "GoalPlannerViewModel uses @Observable and @MainActor"
    - "GoalPlannerView implements the premium Stitch glassmorphic aesthetic"
    - "Data visualisations use concentric circles/segmented neon bars"
  artifacts:
    - path: "Growin/ViewModels/GoalPlannerViewModel.swift"
      provides: "Modernized State Management"
    - path: "Growin/Views/GoalPlannerView.swift"
      provides: "Overhauled Stitch UI"
</must_haves>

<success_criteria>
The Goal Planner UI is fully modernized, performant, and correctly linked to the new `@Observable` ViewModel.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-telemetry/02-02-SUMMARY.md`
</output>
