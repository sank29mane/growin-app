---
phase: 22-leveraged-dividend
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified: [backend/quant_engine.py]
autonomous: true
requirements: [NODE-01, NODE-02]
must_haves:
  truths:
    - "Neural ODE model can predict price recovery velocity post-dividend ex-date"
    - "Adjoint Sensitivity Method ensures training memory efficiency is O(1)"
    - "Recovery trajectory modeling captures continuous-time dynamics better than discrete bins"
  artifacts:
    - path: "backend/quant_engine.py"
      provides: "NeuralODERecovery class"
  key_links:
    - from: "NeuralODERecovery"
      to: "torchdiffeq"
      via: "ODE integration"
---

<objective>
Implement Neural ODE continuous-time modeling for post-dividend price action recovery velocity to enhance exit timing in capture trades.

Purpose: Accurately forecast recovery trajectory and velocity to maximize captured premium.
Output: Neural ODE recovery model in quant engine.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/22-leveraged-dividend/RESEARCH.md
@backend/quant_engine.py
@.planning/phases/22-leveraged-dividend/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Implement NeuralODERecovery Framework</name>
  <files>backend/quant_engine.py</files>
  <action>
    Implement `NeuralODERecovery` class using `torchdiffeq` (install if missing):
    - Define ODE function $f(z(t), t, 	heta)$ to parameterize hidden state derivatives.
    - Implement Adjoint Sensitivity Method for memory efficiency.
    - Train on tick-level data for ex-dividend recovery windows.
    Avoid: Discretizing time into fixed bins; use continuous integration.
  </action>
  <verify>
    Verify model's ability to predict recovery duration with <10% error on historical tick-level data.
  </verify>
  <done>
    `NeuralODERecovery` class implemented and training loop verified.
  </done>
</task>

<task type="auto">
  <name>Integrate Recovery Velocity into Exit Logic</name>
  <files>backend/quant_engine.py</files>
  <action>
    Integrate recovery velocity predictions into `QuantEngine`'s decision logic:
    - Add `predict_recovery_trajectory` method.
    - Use forecasted velocity to set dynamic take-profit levels for dividend capture positions.
  </action>
  <verify>
    Simulation shows improved capture trade returns when using recovery velocity for exit timing.
  </verify>
  <done>
    Exit timing optimized using Neural ODE recovery trajectories.
  </done>
</task>

</tasks>

<verification>
- [ ] Neural ODE model trains successfully with constant memory usage.
- [ ] Predicted recovery trajectories show >0.7 correlation with actual price action in tests.
</verification>

<success_criteria>
- Recovery model achieves O(1) memory cost for training.
- Exit timing based on recovery velocity increases capture yield by >10bp on average.
</success_criteria>

<output>
After completion, create `.planning/phases/22-leveraged-dividend/22-02-SUMMARY.md`
</output>
