---
phase: 23-unified-intelligence
plan: 03
type: execute
wave: 2
depends_on: ["23-01", "23-02"]
files_modified: [backend/utils/portfolio_analyzer.py]
autonomous: true
requirements: [RISK-01]
must_haves:
  truths:
    - "PortfolioAnalyzer correctly calculates time-series metrics (Sharpe, Beta) from daily returns"
    - "Beta calculation uses a valid historical benchmark (e.g. SPY)"
    - "Precision safety is maintained during volatility-weighted aggregation"
  artifacts:
    - path: "backend/utils/portfolio_analyzer.py"
      provides: "PortfolioAnalyzer class"
---

<objective>
Implement institutional-grade portfolio risk metrics based on time-series historical data.

Purpose: Move from cross-sectional snapshots to true performance analysis.
Output: PortfolioAnalyzer in backend/utils/portfolio_analyzer.py.
</objective>

<tasks>
<task type="auto">
  <name>Implement PortfolioAnalyzer</name>
  <files>backend/utils/portfolio_analyzer.py</files>
  <action>
    Create `PortfolioAnalyzer` class:
    - `calculate_daily_returns(price_history)`: Compute log or percentage returns.
    - `calculate_volatility(returns)`: Annualized standard deviation of daily returns.
    - `calculate_sharpe_ratio(returns, risk_free_rate=0.04)`: Standard Sharpe calculation.
    - `calculate_beta(returns, benchmark_returns)`: Regression-based beta against benchmark.
    - `calculate_sortino_ratio(returns)`: Downside-risk weighted performance.
  </action>
</task>
</tasks>
