---
phase: 23-unified-intelligence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [backend/utils/financial_math.py]
autonomous: true
requirements: [INTEL-02, INTEL-03]
must_haves:
  truths:
    - "Financial math library provides 1:1 parity between MLX, Rust, and NumPy backends"
    - "Technical indicators are precision-safe using Decimal where appropriate"
    - "Vectorized operations ensure performance parity with current QuantEngine"
  artifacts:
    - path: "backend/utils/financial_math.py"
      provides: "Unified indicator functions (calculate_rsi, calculate_macd, etc.)"
---

<objective>
Consolidate technical indicator logic (RSI, MACD, SMA, EMA, BBands) into a single vectorized library with multi-backend support.

Purpose: Ensure mathematical consistency and simplify QuantEngine logic.
Output: Centralized TechnicalIndicators library in backend/utils/financial_math.py.
</objective>

<tasks>
<task type="auto">
  <name>Consolidate Technical Indicators</name>
  <files>backend/utils/financial_math.py</files>
  <action>
    Implement unified indicator functions with backend routing:
    - `calculate_rsi(prices, period, backend='auto')`: Routes to MLX, Rust, or NumPy.
    - `calculate_macd(...)`, `calculate_sma(...)`, etc.
    - Ensure all backends return identical results within 1e-6 tolerance.
    - Use `Decimal` for final signal thresholds but NumPy/MLX for series computation.
  </action>
</task>
</tasks>
