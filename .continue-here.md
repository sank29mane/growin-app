# Handoff: Phase 30 - High-Velocity Intra-day Trading (LETFs)

## 📅 Timestamp
Wednesday, March 11, 2026

## 📍 Current Position
**Phase 30: High-Velocity Intra-day Trading (LETFs)**
We have successfully pivoted from Multi-Asset (Commodities) to a high-velocity intraday LETF strategy (TQQQ/SQQQ) to maximize daily profits within Trading 212 Beta API constraints. The core compliance architecture (Request Budgeter + HITL Gate) is implemented and the UI is ready to render trade proposals.

## ✅ Completed Work
- **Research Synchronization**: Extracted a comprehensive compliance report from deep research and archived it in the "Growin Research" NotebookLM.
- **T212 Request Budgeter**: Implemented a priority-aware Token Bucket (`backend/utils/rate_limiter.py`) and integrated it into the MCP server. Outgoing requests are now governed by a 20-ticker/5s budget with Execution > Sync > Polling priority.
- **HITL Core**: Updated `DecisionAgent` to parse natural language trade setups into structured `TradeProposalData`.
- **Telemetry Handshake**: `OrchestratorAgent` now broadcasts `rebalance_proposal` events to the frontend.
- **SwiftUI Trade Cards**: Implemented and integrated the `TradeProposalCard` into `RichMessageComponents.swift` and `ChatViewModel.swift`. The UI kinetic-renders a premium "NPU Trade Proposal" card when an opportunity is detected.
- **CI Stability**: Fixed consistent CI failures by resolving hidden merge conflicts, restoring missing `GeopoliticalData`/`RiskGovernanceData` models, and fixing `PYTHONPATH` in `.github/workflows/ci.yml`.
- **Repo Cleanup**: Consolidated all tests into `tests/backend/`, archived old phases, and removed technical debt scripts.

## 🛠 Remaining Work
- **Approval Execution**: Finish the `onApprove` and `onReject` closures in `RichMessageComponents.swift`. They need to call a new backend endpoint to finalize the trade.
- **Backend Approval Endpoint**: Implement a route in `routes/ai_routes.py` (or similar) that validates the HMAC-signed approval and triggers the actual T212 order tool.
- **Intraday Model Tuning**: Port/Adapt the `NeuralJMCE` for 5-minute tick covariance shift detection (Track 3 of `30-01-PLAN.md`).
- **ORB Triggers**: Define and implement Opening Range Breakout (ORB) logic for TQQQ/SQQQ.

## 🧠 Key Decisions
- **Token Bucket Algorithm**: Chosen for its burst-tolerant nature, which fits Trading 212's API behavior.
- **Serial Execution + Jitter**: Decisions made to satisfy T212's "human-organic" order flow expectations and prevent "Suspicious Algorithmic Trading" flags.
- **No Crypto**: Strict constraint maintained; all crypto code and research removed.

## 🚧 Blockers
- None.

## 🚀 Resume Instructions
1. Check out the current branch (`main` is clean and updated).
2. Start by implementing the frontend-to-backend approval handshake:
   - Update `RichMessageComponents.swift` to send a POST request to `/api/ai/trade/approve`.
   - Implement that endpoint in the FastAPI backend to verify the proposal and call `mcp.place_market_order`.
3. Proceed to **Track 3** of the plan: adapting the MLX model for 5-minute resolution.
