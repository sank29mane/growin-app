# Handoff: Phase 30 - High-Velocity Intra-day Trading (LETFs)

## 📅 Timestamp
Thursday, March 12, 2026

## 📍 Current Position
**Phase 30: High-Velocity Intra-day Trading (LETFs)**
We have successfully implemented the full HITL (Human-in-the-loop) trade approval handshake, adapted the MLX NeuralJMCE model for high-velocity resolution, and established an **ANE-ready architecture** for local NPU offloading of quantitative models.

## ✅ Completed Work
- **HITL Approval Handshake**: 
    - Added `/api/ai/trade/approve` and `/api/ai/trade/reject` endpoints in `ai_routes.py`.
    - Integrated with Trading 212 MCP `place_market_order` tool.
    - Updated `ChatViewModel.swift` and `RichMessageComponents.swift` to handle approval actions and reflect status in the UI.
    - Verified endpoints with `tests/backend/test_hitl_trade_routes.py`.
- **High-Velocity Model Adaptation (Track 3)**:
    - Updated `NeuralJMCE` in `jmce_model.py` with `TimeResolution` (1Min/5Min) support.
    - Added **Covariance Velocity** tracking for tick-level shift detection.
    - Implemented `ORBDetector` in `backend/utils/orb_detector.py` for Opening Range Breakouts.
    - Verified model output shapes and positive definiteness with `tests/backend/test_neural_jmce_intraday.py`.
- **ANE Readiness Architecture**:
    - Implemented `get_jmce_model()` factory to prioritize ANE (CoreML) over GPU (MLX) for quantitative analysis.
    - Added `CoreMLJMCE` wrapper to bridge the transformer backbone to dedicated Neural Engine hardware.
    - Updated `PortfolioAnalyzer` to dynamically switch between MLX and CoreML compute paths.
- **System Stability**:
    - Fixed `NameError: name 'Any' is not defined` in `backend/utils/rate_limiter.py`.
    - Resolved `PYTHONPATH` issues in the local testing environment.

## 🛠 Remaining Work
- **Decision Logic Integration**: Update `DecisionAgent` to actively trigger `ORBDetector` when TQQQ/SQQQ are in context.
- **CoreML Conversion Script**: Implement a script to export the `NeuralJMCE` weights to `.mlmodel` format to finalize the ANE transition.
- **High-Velocity Backtest**: Implement a simulation script to verify the TQQQ/SQQQ strategy using the new NPU-accelerated JMCE.
- **Dynamic Fee Model**: Add Trading 212 spread/commission logic to the alpha hurdle in `PortfolioAnalyzer`.

## 🧠 Key Decisions
- **Covariance Velocity**: Added as a first-class model output to provide earlier warnings of regime shifts than raw covariance.
- **Hybrid NPU Strategy**: LLMs (Mistral/Granite) remain on the **GPU (MLX)** for throughput, while Intraday Quant models prioritize the **ANE (CoreML)** for latency and efficiency.
- **HITL Persistence**: Trade proposals are stored in `AppState.trade_proposals` to ensure state consistency across the user session.

## 🚧 Blockers
- None.

## 🚀 Resume Instructions
1. Implement the **CoreML conversion script** to move the JMCE weights to the ANE.
2. Integrate `ORBDetector` into `DecisionAgent.py` to trigger live trade proposals.
3. Run a high-velocity backtest to verify the strategy's alpha under the new fee model.
