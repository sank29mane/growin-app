---
phase: 23-unified-intelligence
plan: 05
type: execute
wave: 3
depends_on: ["23-01", "23-02", "23-03", "23-04"]
files_modified: [backend/quant_engine.py, backend/agents/quant_agent.py, backend/agents/portfolio_agent.py, backend/agents/coordinator_agent.py]
autonomous: true
requirements: [REFACT-01]
must_haves:
  truths:
    - "Redundant math and ticker logic is deleted from at least 3 agent files"
    - "Agents correctly import and utilize the unified libraries"
    - "System performance remains consistent after refactoring"
---

<objective>
Refactor the entire backend system to use the new unified intelligence and analytics libraries.

Purpose: Reduce technical debt, improve maintainability, and ensure cross-agent consistency.
Output: Refactored QuantEngine, QuantAgent, PortfolioAgent, and CoordinatorAgent.
</objective>

<tasks>
<task type="auto">
  <name>System-Wide Refactoring</name>
  <files>backend/quant_engine.py, backend/agents/quant_agent.py, backend/agents/portfolio_agent.py, backend/agents/coordinator_agent.py</files>
  <action>
    Update agents and engine to use centralized utilities:
    - Replace local `normalize_ticker` imports with `TickerResolver`.
    - Replace inline RSI/MACD/SMA logic with `TechnicalIndicators` library.
    - Migrate portfolio risk metric calls to `PortfolioAnalyzer`.
    - Remove redundant code and update tests to match new interfaces.
  </action>
</task>
</tasks>
