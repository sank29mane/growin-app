---
phase: 23-unified-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/utils/ticker_utils.py]
autonomous: true
requirements: [INTEL-01]
must_haves:
  truths:
    - "TickerResolver correctly identifies US vs UK exchanges without regex heuristics"
    - "NLP extraction accurately finds tickers in natural language strings"
    - "Tiered resolution properly caches and validates tickers against providers"
  artifacts:
    - path: "backend/utils/ticker_utils.py"
      provides: "TickerResolver class"
---

<objective>
Implement a unified TickerResolver service to centralize normalization, extraction, and validation logic.

Purpose: Eliminate ticker resolution fragility across the entire system.
Output: Centralized TickerResolver in backend/utils/ticker_utils.py.
</objective>

<tasks>
<task type="auto">
  <name>Implement TickerResolver class</name>
  <files>backend/utils/ticker_utils.py</files>
  <action>
    Refactor existing `normalize_ticker` and create `TickerResolver` class:
    - `normalize(ticker)`: Centralized US/UK/Leveraged logic (preferring Rust core).
    - `extract(text)`: NLP-based ticker extraction (handling "Compare AAPL and MSFT").
    - `resolve(query)`: Tiered resolution: Cache -> Provider (Alpaca/Finnhub) Verify -> Search.
    - Standardize all special mappings and exclusion lists into this class.
  </action>
</task>
</tasks>
