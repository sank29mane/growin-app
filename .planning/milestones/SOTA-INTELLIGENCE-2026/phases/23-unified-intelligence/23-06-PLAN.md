---
phase: 23-unified-intelligence
plan: 06
type: execute
wave: 3
depends_on: ["23-01", "23-02", "23-03", "23-04"]
files_modified: [tests/backend/test_unified_intelligence.py]
autonomous: true
requirements: [INTEL-03]
must_haves:
  truths:
    - "System functions correctly even when optional dependencies (MLX, Scipy) are missing"
    - "Math fallbacks are mathematically identical to optimized versions"
    - "Cross-platform tests pass on Apple Silicon and Linux"
---

<objective>
Implement a comprehensive fallback testing strategy to ensure mathematical and functional consistency across different environments.

Purpose: Guarantee system reliability when running without hardware acceleration or specialized libraries.
Output: Missing-dependency test suite in tests/backend/test_unified_intelligence.py.
</objective>

<tasks>
<task type="auto">
  <name>Implement Fallback Verification</name>
  <files>tests/backend/test_unified_intelligence.py</files>
  <action>
    Create a robust test suite that:
    - Mocks the absence of `mlx`, `growin_core`, and `scipy`.
    - Verifies that all technical indicators and portfolio metrics still calculate correctly.
    - Ensures results from pure-Python/NumPy paths match optimized versions within 1e-6 tolerance.
    - Validates error handling for cases with truly insufficient data.
  </action>
</task>
</tasks>
