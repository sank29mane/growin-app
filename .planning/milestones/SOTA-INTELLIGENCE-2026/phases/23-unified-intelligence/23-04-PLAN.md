---
phase: 23-unified-intelligence
plan: 04
type: execute
wave: 2
depends_on: ["23-03"]
files_modified: [backend/utils/portfolio_analyzer.py]
autonomous: true
requirements: [RISK-02]
must_haves:
  truths:
    - "Backcast engine can approximate historical portfolio value using position entry dates"
    - "Generated history aligns with available actual broker data where they overlap"
  artifacts:
    - path: "backend/utils/portfolio_analyzer.py"
      provides: "BackcastEngine class or methods"
---

<objective>
Implement a 'Backcast' engine to generate synthetic historical performance for portfolios with incomplete transaction data.

Purpose: Enable immediate risk analysis for new accounts without waiting for months of live data.
Output: Backcast functionality integrated into PortfolioAnalyzer.
</objective>

<tasks>
<task type="auto">
  <name>Implement Backcast Engine</name>
  <files>backend/utils/portfolio_analyzer.py</files>
  <action>
    Implement `generate_backcast_history(positions, period_days)`:
    - Use historical price data for each position's ticker.
    - Factor in position entry dates (if available) or assume current quantities.
    - Produce a daily time-series of total portfolio value.
    - Ensure results are consumable by `calculate_sharpe_ratio` and `calculate_beta`.
  </action>
</task>
</tasks>
