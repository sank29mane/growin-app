---
phase: 22-leveraged-dividend
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified: [backend/quant_engine.py]
autonomous: true
requirements: [SIM-01, SIM-02]
must_haves:
  truths:
    - "Simulation engine can perform 1000x faster inference via GPU acceleration"
    - "Hybrid MC-ML framework can predict tail-losses and black-swan anomalies"
    - "Multi-factor stressors correctly impact simulation paths"
  artifacts:
    - path: "backend/quant_engine.py"
      provides: "SimulationEngine class"
  key_links:
    - from: "SimulationEngine"
      to: "MLX / PyTorch / JAX"
      via: "GPU-accelerated ops"
---

<objective>
Build a Hybrid Monte Carlo + Machine Learning (MC-ML) simulation framework for large-scale portfolio stress testing with 1000x faster agentic GPU acceleration.

Purpose: Provide deep risk visibility and stress-testing capabilities for leveraged portfolios.
Output: GPU-accelerated Hybrid SimulationEngine in quant engine.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/22-leveraged-dividend/RESEARCH.md
@backend/quant_engine.py
@.planning/phases/22-leveraged-dividend/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Implement GPU-Accelerated Simulation Engine</name>
  <files>backend/quant_engine.py</files>
  <action>
    Implement `SimulationEngine` class utilizing MLX (for Apple Silicon) or PyTorch (fallback):
    - Build vectorized Monte Carlo path generation for assets.
    - Implement joint stressing of yield curves, basis risks, and liquidity.
    - Achieve 1000x speedup over sequential CPU-based simulation.
    Avoid: Loop-based simulation logic; use matrix/tensor operations exclusively.
  </action>
  <verify>
    Measure speedup vs. standard CPU implementation for 100,000 paths; target 1000x improvement.
  </verify>
  <done>
    `SimulationEngine` implemented with verified GPU acceleration.
  </done>
</task>

<task type="auto">
  <name>Implement ML-Based Tail-Loss & Stress Overlay</name>
  <files>backend/quant_engine.py</files>
  <action>
    Add ML-based forecasting to the simulation:
    - Integrate XGBoost/LightGBM for forecasting tail-loss (CVaR) probabilities.
    - Implement unsupervised anomaly detection to identify "black-swan" scenarios in paths.
    - Create `simulate_stress_test` method that combines MC paths with ML risk overlays.
  </action>
  <verify>
    Compare simulation tail-loss predictions against historical stress events (e.g., 2020 crash).
  </verify>
  <done>
    Hybrid MC-ML simulation framework verified against historical stress scenarios.
  </done>
</task>

</tasks>

<verification>
- [ ] Simulation engine performs 1,000,000 path updates in under 1 second (GPU).
- [ ] ML overlays correctly flag known historical stress events as high-risk anomalies.
</verification>

<success_criteria>
- Achievement of 1000x speedup in simulation inference.
- Tail-loss prediction accuracy improved by >20% compared to pure MC.
</success_criteria>

<output>
After completion, create `.planning/phases/22-leveraged-dividend/22-03-SUMMARY.md`
</output>
