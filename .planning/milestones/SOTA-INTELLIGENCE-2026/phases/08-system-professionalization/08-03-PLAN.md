---
phase: 08-system-professionalization
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified: ["backend/routes/status_routes.py", "backend/market_context.py"]
autonomous: true
requirements: [PROF-03]
---

<objective>
Implement the Reasoning Trace API and final verification.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Reasoning Trace API</name>
  <action>
    Add an endpoint to `status_routes.py` (or a new telemetry router):
    - `GET /api/telemetry/trace/{request_id}`
    - It should fetch the history from `Messenger` for that ID and return a structured summary.
  </action>
</task>

<task type="auto">
  <name>Task 2: Final Verification & Documentation</name>
  <action>
    - Run the integrity check on the audit log.
    - Document the new SSE event types for the frontend team.
    - Create a final summary for Phase 08.
  </action>
</task>

</tasks>

<verification>
- Verify the trace endpoint returns a coherent list of agent actions.
</verification>
