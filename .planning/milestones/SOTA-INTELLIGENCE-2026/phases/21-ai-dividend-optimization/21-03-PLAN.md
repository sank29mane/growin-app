---
phase: 21-ai-dividend-optimization
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - Growin/Views/PassiveIncomeDashboard.swift
  - Growin/ViewModels/PassiveIncomeViewModel.swift
  - Growin/Services/PushNotificationService.swift
autonomous: false
requirements: [DIV-06, DIV-07]
must_haves:
  truths:
    - Probability cloud visualizing TTM-R2/XGBoost ranges is displayed
    - Solid green (settled) and soft blue (predicted) income tracked clearly
    - One-tap approval cards exist for HITL panic/abort interventions
  artifacts:
    - path: Growin/Views/PassiveIncomeDashboard.swift
      provides: Liquid Glass SwiftUI command center
    - path: Growin/ViewModels/PassiveIncomeViewModel.swift
      provides: State management bridging agent telemetry to UI
  key_links:
    - from: Growin/Views/PassiveIncomeDashboard.swift
      to: Growin/ViewModels/PassiveIncomeViewModel.swift
      via: @StateObject / @ObservedObject bindings
---

<objective>
Implement the Command Center UI for passive income tracking and agent supervision.

Purpose: Provide the user with a "Liquid Glass" aesthetic interface to visualize dividend capture probability clouds and quickly approve/abort AI-recommended trades.
Output: `PassiveIncomeDashboard.swift`, underlying ViewModel, and push notification wiring.
</objective>

<context>
@.planning/phases/21-ai-dividend-optimization/RESEARCH.md
@.planning/phases/21-ai-dividend-optimization/CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: PassiveIncomeViewModel & Notification Service</name>
  <files>Growin/ViewModels/PassiveIncomeViewModel.swift, Growin/Services/PushNotificationService.swift</files>
  <action>Create the ViewModel to parse consensus metrics and predicted income vs settled income. Add push notification scaffolding in `PushNotificationService` specifically for "Gauge & Abort" triggers requiring manual HITL approval.</action>
  <verify>xcodebuild clean build -project Growin.xcodeproj -scheme Growin</verify>
  <done>ViewModel and notification service compile successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Build PassiveIncomeDashboard UI</name>
  <files>Growin/Views/PassiveIncomeDashboard.swift</files>
  <action>Implement the dashboard using the Liquid Glass aesthetic. Create the Probability Cloud chart (translucent consensus range overlay). Add the 'Quick-Action HITL Card' component for one-tap approvals with a Reasoning Chip. Add specific badge icons for dividend stocks in the portfolio list.</action>
  <verify>xcodebuild clean build -project Growin.xcodeproj -scheme Growin</verify>
  <done>Dashboard UI compiles without errors and binds to the ViewModel.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>SwiftUI Passive Income Command Center with probability clouds and HITL quick-action cards.</what-built>
  <how-to-verify>
    1. Open the app in Xcode Simulator.
    2. Navigate to the Passive Income Dashboard.
    3. Verify Liquid Glass styling, Probability Cloud rendering, and the Quick-Action approval card presence.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>
</tasks>
