---
phase: 02-frontend-telemetry
plan: 03
type: execute
wave: 2
depends_on: [02-02]
files_modified: [Growin/Views/DashboardView.swift, Growin/Views/PortfolioView.swift, Growin/Views/StockChartView.swift]
autonomous: true
requirements: [FE-03]
---

<objective>
Comprehensively overhaul the core dashboard, portfolio, and stock chart views using the Stitch Design System. This includes implementing premium typography, neon accents, and refined glassmorphic components to ensure a consistent, ultra-high-end visual experience across the entire app.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@Growin/ThemeComponents.swift
@Growin/Color+Extension.swift
@Growin/Views/DashboardView.swift
@Growin/Views/PortfolioView.swift
@Growin/Views/StockChartView.swift
</context>

<tasks>

<task type="auto">
  <name>Overhaul DashboardView with Stitch Design</name>
  <files>Growin/Views/DashboardView.swift</files>
  <action>
    - Update `DashboardView` to use `premiumTypography` throughout.
    - Replace standard system colors with Stitch neon palette (e.g., `stitchNeonIndigo` for allocation charts).
    - Refine `MetricGrid` and `MiniMetricCard` with updated spacing and typography.
    - Enhance `AccountSectionView` with more distinct Stitch card styling.
    - Ensure responsive spacing for macOS and iOS.
  </action>
  <verify>
    Check for `premiumTypography` usage and Stitch color constants in `DashboardView.swift`.
  </verify>
  <done>
    `DashboardView` reflects the premium Stitch aesthetic.
  </done>
</task>

<task type="auto">
  <name>Overhaul PortfolioView with Stitch Design</name>
  <files>Growin/Views/PortfolioView.swift</files>
  <action>
    - Update `PortfolioView` and its subviews (`MetricGrid`, `MiniMetricCard`, `PositionDeepCard`) to use `premiumTypography`.
    - Replace system background colors with `GradientBackground` or `MeshBackground`.
    - Update `PerformanceLineChart` and `AllocationDonutChart` to use Stitch neon gradients and colors.
    - Refine the header with Stitch styling and updated button components.
  </action>
  <verify>
    Check for `premiumTypography` and Stitch color usage in `PortfolioView.swift`.
  </verify>
  <done>
    `PortfolioView` is fully integrated into the Stitch Design System.
  </done>
</task>

<task type="auto">
  <name>Overhaul StockChartView with Stitch Design</name>
  <files>Growin/Views/StockChartView.swift</files>
  <action>
    - Update `StockChartView` to use `premiumTypography`.
    - Refine the header to use larger, bolder typography for the symbol and price.
    - Update the main chart with more vibrant neon colors (`stitchNeonGreen` / `stitchNeonRed`) and glowing area fills.
    - Overhaul the "Neural Insight" and "Quant Vectors" cards with high-fidelity Stitch components.
    - Update the timeframe picker with Stitch-style capsular buttons.
  </action>
  <verify>
    Check for `premiumTypography` and Stitch color usage in `StockChartView.swift`.
  </verify>
  <done>
    `StockChartView` delivers a pro-grade terminal feel using the Stitch palette.
  </done>
</task>

</tasks>

<must_haves>
  truths:
    - "Core views use premiumTypography instead of system fonts"
    - "Charts utilize the Stitch neon color palette"
    - "UI components exhibit consistent glassmorphism and neon accents"
  artifacts:
    - path: "Growin/Views/DashboardView.swift"
      provides: "Stitch Dashboard"
    - path: "Growin/Views/PortfolioView.swift"
      provides: "Stitch Portfolio"
    - path: "Growin/Views/StockChartView.swift"
      provides: "Stitch Stock Charts"
</must_haves>

<success_criteria>
All primary financial views (Dashboard, Portfolio, Stock Charts) are visually consistent with the Goal Planner overhaul and adhere to the premium Stitch Design System.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-telemetry/02-03-SUMMARY.md`
</output>
