---
phase: 35-multi-modal-context-infusion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/mlx_vlm_engine.py, backend/utils/image_proc.py, tests/vision/test_image_proc.py, tests/vision/test_vlm_engine.py]
autonomous: true
requirements: [VLM-01]

must_haves:
  truths:
    - "A chart image can be successfully processed and described by the local vision model"
    - "Visual patterns are accurately highlighted on the original chart for the user"
  artifacts:
    - path: "backend/mlx_vlm_engine.py"
      provides: "VLM model loading and generation"
    - path: "backend/utils/image_proc.py"
      provides: "Image loading and normalization"
    - path: "tests/vision/test_image_proc.py"
      provides: "Unit tests for image preprocessing"
    - path: "tests/vision/test_vlm_engine.py"
      provides: "Unit tests for VLM inference"
---

<objective>
Implement the core VLM inference engine, image preprocessing utility, and associated unit tests.
Purpose: Provide Metal-accelerated vision capabilities to the backend with verified accuracy.
Output: Working MLXVLMInferenceEngine, image helper, and green unit tests.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/35-multi-modal-context-infusion/RESEARCH.md
@.planning/phases/35-multi-modal-context-infusion/CONTEXT.md
@.planning/phases/35-multi-modal-context-infusion/35-VALIDATION.md
@backend/mlx_engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Image Processing Utility and Tests</name>
  <files>backend/utils/image_proc.py, tests/vision/test_image_proc.py</files>
  <action>
    1. Create `backend/utils/image_proc.py` to load, resize (preserving aspect ratio), and normalize images for VLM consumption using Pillow.
    2. Implement `prepare_vlm_image(image_path_or_bytes)` to return a preprocessed image and original dimensions.
    3. Create `tests/vision/test_image_proc.py` with tests for aspect ratio preservation and dimension normalization.
  </action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/vision/test_image_proc.py</automated>
  </verify>
  <done>Utility file and unit tests created; tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Create MLX VLM Inference Engine and Tests</name>
  <files>backend/mlx_vlm_engine.py, tests/vision/test_vlm_engine.py</files>
  <action>
    1. Implement `MLXVLMInferenceEngine` in `backend/mlx_vlm_engine.py` using `mlx_vlm.load` for `mlx-community/Qwen2.5-VL-7B-Instruct-4bit`.
    2. Implement `generate(image, prompt)` with memory monitoring and absolute coordinate support.
    3. Create `tests/vision/test_vlm_engine.py` to verify model loading and basic description generation.
  </action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/vision/test_vlm_engine.py</automated>
  </verify>
  <done>VLM engine wrapper and unit tests implemented; tests passing.</done>
</task>

</tasks>

<verification>
Check if `mlx-vlm` and `Pillow` are installed in the environment.
</verification>

<success_criteria>
Backend can programmatically load a VLM model and run a simple "describe this image" prompt, verified by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/35-multi-modal-context-infusion/35-01-SUMMARY.md`
</output>
