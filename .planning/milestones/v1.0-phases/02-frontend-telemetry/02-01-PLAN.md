---
phase: 02-frontend-telemetry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [Growin/Color+Extension.swift, Growin/ThemeComponents.swift]
autonomous: true
requirements: [FE-04]
---

<objective>
Establish the Stitch Design System foundation by updating core colors, typography, and glassmorphism components.

Purpose: Provide the visual primitives required for all subsequent UI tasks.
Output: Updated color palette and reusable glassmorphism components in SwiftUI.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@ARCHITECTURE.md
@Growin/Color+Extension.swift
@Growin/ThemeComponents.swift
</context>

<tasks>

<task type="auto">
  <name>Update Stitch Color Palette</name>
  <files>Growin/Color+Extension.swift</files>
  <action>
    Update the `Color` extension to implement the Stitch neon color palette and deep charcoal backgrounds.
    - Replace `growinDarkBg` with a deep charcoal (e.g., #0A0A0B).
    - Add Stitch Neon colors: `stitchNeonIndigo`, `stitchNeonCyan`, `stitchNeonPurple`, `stitchNeonGreen`, `stitchNeonYellow`.
    - Update `textPrimary` and `textSecondary` for premium typography contrast.
    - Ensure all hex codes are accurate to a modern dark theme.
  </action>
  <verify>
    Check file for new color constants and run a basic SwiftUI preview if possible.
  </verify>
  <done>
    `Color+Extension.swift` contains the full Stitch color palette.
  </done>
</task>

<task type="auto">
  <name>Refactor Glassmorphism & Typography</name>
  <files>Growin/ThemeComponents.swift</files>
  <action>
    Overhaul `ThemeComponents.swift` to match the Stitch design.
    - Refactor `GlassCard` to use deeper backgrounds, more subtle borders, and neon glow hover effects.
    - Update `MeshBackground` to use the new Stitch neon colors and adjusted blur/opacity for a more premium feel.
    - Create a `PremiumTypography` modifier or extension to handle specialized font weights and tracking (kerning) for that "premium" feel.
    - Update `PremiumButton` to use neon accents and smooth scaling animations.
  </action>
  <verify>
    Verify that `ThemeComponents.swift` uses the new Stitch colors and has updated glass effects.
  </verify>
  <done>
    `ThemeComponents.swift` provides updated reusable components adhering to the Stitch design.
  </done>
</task>

</tasks>

<must_haves>
  truths:
    - "App theme uses deep charcoals instead of slate"
    - "UI elements exhibit glassmorphism with subtle neon accents"
    - "Typography feels premium with custom tracking and weighting"
  artifacts:
    - path: "Growin/Color+Extension.swift"
      provides: "Stitch Color Palette"
    - path: "Growin/ThemeComponents.swift"
      provides: "Stitch Reusable Components"
  key_links:
    - from: "Growin/ThemeComponents.swift"
      to: "Growin/Color+Extension.swift"
      via: "Color constants"
</must_haves>

<success_criteria>
Foundation components are ready for use in views, with all "Stitch" colors and glass effects implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-telemetry/02-01-SUMMARY.md`
</output>
