---
phase: 35-multi-modal-context-infusion
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [backend/market_context.py, backend/agents/vision_agent.py, tests/vision/test_vision_agent.py]
autonomous: true
requirements: [VLM-02]

must_haves:
  truths:
    - "The system successfully extracts identifiable technical patterns from chart screenshots"
  artifacts:
    - path: "backend/market_context.py"
      provides: "New VisionData schema"
    - path: "backend/agents/vision_agent.py"
      provides: "VLM agent wrapper for the swarm"
    - path: "tests/vision/test_vision_agent.py"
      provides: "Unit tests for vision agent logic"
---

<objective>
Implement the VisionAgent and update the MarketContext to support multi-modal data.
Purpose: Allow the Multi-Agent Swarm to process and store visual technical analysis.
Output: VisionAgent capable of populating MarketContext with pattern signals.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/35-multi-modal-context-infusion/RESEARCH.md
@.planning/phases/35-multi-modal-context-infusion/35-VALIDATION.md
@backend/market_context.py
@backend/agents/base_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MarketContext Schema</name>
  <files>backend/market_context.py</files>
  <action>
    Add `VisionData` and `VisualPattern` Pydantic models to `market_context.py`.
    - `VisualPattern`: name, confidence, bbox_2d, reasoning.
    - `VisionData`: patterns (List), raw_description, timestamp.
    - Add `vision: Optional[VisionData]` to the main `MarketContext` model.
  </action>
  <verify>
    <automated>python3 -c "from market_context import MarketContext, VisionData; MarketContext(query='test', vision=VisionData(patterns=[], raw_description='test'))"</automated>
  </verify>
  <done>MarketContext updated with vision-specific fields.</done>
</task>

<task type="auto">
  <name>Task 2: Implement VisionAgent and Tests</name>
  <files>backend/agents/vision_agent.py, tests/vision/test_vision_agent.py</files>
  <action>
    1. Create `backend/agents/vision_agent.py` inheriting from `BaseAgent`.
    2. Implement `analyze(image_data)` using `MLXVLMInferenceEngine`.
    3. Use Magentic/Pydantic to structure the VLM output into `VisionData`.
    4. Create `tests/vision/test_vision_agent.py` to verify agent logic and MarketContext population.
  </action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/vision/test_vision_agent.py</automated>
  </verify>
  <done>VisionAgent implemented and verified with unit tests.</done>
</task>

</tasks>

<verification>
Check if `MarketContext` can be instantiated with `VisionData`.
</verification>

<success_criteria>
The multi-agent system has a specialized VisionAgent that can describe a chart and identify at least one technical pattern in a structured format.
</success_criteria>

<output>
After completion, create `.planning/phases/35-multi-modal-context-infusion/35-02-SUMMARY.md`
</output>
