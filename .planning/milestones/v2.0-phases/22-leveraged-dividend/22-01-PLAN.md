---
phase: 22-leveraged-dividend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/dividend_bridge.py, backend/quant_engine.py]
autonomous: true
requirements: [LDC-01, LDC-02]
must_haves:
  truths:
    - "Leveraged dividend engine can calculate optimal entry/exit for ex-day premiums"
    - "Hedging logic correctly calculates Delta-Neutral call option overlays"
    - "Index Future Netting accurately identifies systemic risk reduction amount"
  artifacts:
    - path: "backend/dividend_bridge.py"
      provides: "LeveragedDividendEngine class"
    - path: "backend/quant_engine.py"
      provides: "IndexNetting logic"
  key_links:
    - from: "LeveragedDividendEngine"
      to: "backend/data_models.py:DividendData"
      via: "data ingestion"
---

<objective>
Implement the Leveraged Dividend Capture (LDC) Engine to target ex-day premiums with institutional-grade execution alpha and systemic risk hedging.

Purpose: Achieve 40bp execution gap and neutralize systemic market risk during dividend capture trades.
Output: LeveragedDividendEngine and hedging integration in backend.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/22-leveraged-dividend/RESEARCH.md
@backend/dividend_bridge.py
@backend/quant_engine.py
</context>

<tasks>

<task type="auto">
  <name>Implement LeveragedDividendEngine</name>
  <files>backend/dividend_bridge.py</files>
  <action>
    Extend `DividendBridge` or create `LeveragedDividendEngine` class to:
    - Implement solver-based routing logic targeting 40bp execution gap.
    - Calculate optimal leverage ratios based on ex-day premium statistical anomalies.
    - Implement ex-date entry/exit timing using price-action triggers.
    Avoid: Simple fixed-leverage approaches; use dynamic volatility-adjusted sizing.
  </action>
  <verify>
    Run tests ensuring `LeveragedDividendEngine` calculates entry/exit within 40bp of theoretical optimum on historical ex-dividend data.
  </verify>
  <done>
    `LeveragedDividendEngine` implemented and verified with historical backtest data.
  </done>
</task>

<task type="auto">
  <name>Implement Delta-Neutral & Index Netting Hedging</name>
  <files>backend/quant_engine.py</files>
  <action>
    Add `hedging_manager` functionality to `QuantEngine`:
    - `calculate_delta_neutral_overlay`: Recommends OTM call strikes and quantities to neutralize position delta.
    - `calculate_index_netting`: Calculates necessary index future short positions to remove systemic market risk (beta-neutralization).
    - Ensure integration with `DividendData` types.
  </action>
  <verify>
    Verify that `calculate_index_netting` reduces portfolio beta by >90% in simulated trades.
  </verify>
  <done>
    Hedging logic integrated into `QuantEngine` and verified via beta-reduction metrics.
  </done>
</task>

</tasks>

<verification>
- [ ] LeveragedDividendEngine can ingest dividend history and output trade plans.
- [ ] Hedging overlays correctly reduce systemic risk in test scenarios.
</verification>

<success_criteria>
- Leveraged capture engine achieves target 40bp execution gap in simulations.
- Beta-netting reduces capture trade volatility by at least 50%.
</success_criteria>

<output>
After completion, create `.planning/phases/22-leveraged-dividend/22-01-SUMMARY.md`
</output>
