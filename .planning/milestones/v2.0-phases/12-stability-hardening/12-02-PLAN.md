---
phase: 12-stability-hardening
plan: 02
type: execute
wave: 1
depends_on: ["12-01"]
files_modified: [
    "docs/ARCHITECTURE.md",
    "Growin/Views/ContentView.swift",
    "Growin/Views/IntelligentConsoleView.swift"
]
autonomous: true
requirements: [STAB-04]
---

<objective>
Purge all iOS-specific traces and verify the application as a pure-play macOS native application using AppKit-compatible patterns.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Audit & Purge iOS Framework References</name>
  <action>
    - Conduct a deep scan for `UIKit`, `UIApplication`, or `UI` prefix types in the source code.
    - Replace or isolate with `#if os(macOS)` any mobile-only logic.
    - Update `docs/ARCHITECTURE.md` to correctly reference `NSViewRepresentable` for Metal integrations instead of `UIViewRepresentable`.
  </action>
</task>

<task type="auto">
  <name>Task 2: Standardise macOS UI Modifiers</name>
  <action>
    - Review `ContentView.swift` and other views for mobile-specific modifiers.
    - Ensure window-level interactions use `AppKit` (e.g., `NSWindow`) if necessary, or pure SwiftUI macOS APIs.
    - Verify that `.scrollContentBackground(.hidden)` and similar modifiers are correctly applied for the macOS styling.
  </action>
</task>

<task type="auto">
  <name>Task 3: Verify Platform Build Settings</name>
  <action>
    - Double-check `project.pbxproj` to ensure no iOS-only frameworks are being linked.
    - Verify that `TARGETED_DEVICE_FAMILY` is strictly set for macOS (4).
  </action>
</task>

</tasks>

<verification>
- **Framework Check**: `grep` should return zero results for `UIKit` imports in the `Growin/` source directory.
- **Documentation**: Architecture docs should accurately reflect the macOS native stack.
- **Build**: Application should compile and run without warnings related to platform-specific API usage.
</verification>
