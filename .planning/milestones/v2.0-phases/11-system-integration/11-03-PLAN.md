---
phase: 11-system-integration
plan: 03
type: execute
wave: 1
depends_on: ["11-02"]
files_modified: [
    "backend/tests/test_performance_benchmarks.py",
    ".github/workflows/ci.yml"
]
autonomous: true
requirements: [SYS-05, SYS-01]
---

<objective>
Establish performance benchmarks for SOTA components and integrate the full test suite into the CI/CD pipeline for automated verification.
</objective>

<tasks>

<task type="auto" status="completed">
  <name>Task 1: Implement Backend Performance Benchmarks</name>
  <action>
    - Create `backend/tests/test_performance_benchmarks.py` to measure:
        -   **R-Stitch Speedup**: Benchmark SLM/LLM hybrid vs. full LLM trajectories.
        -   **SSE Latency**: Measure time-to-first-token and event emission frequency.
        -   **CDC Sync Delta**: Measure latency of delta synchronization.
    - Assert that benchmarks meet defined SOTA targets (e.g., >3x R-Stitch speedup, <100ms SSE latency).
  </action>
</task>

<task type="auto" status="completed">
  <name>Task 2: Integrate CI/CD for Automated Testing</name>
  <action>
    - **SOTA Task**: Create/Update `.github/workflows/ci.yml` (or equivalent) to:
        -   Run `uv sync` to manage dependencies.
        -   Execute all `pytest` suites (unit, integration, E2E, performance).
        -   Include **DeepEval** evaluations for reasoning trace quality.
        -   (Optional) Configure Xcode Cloud or similar for `XCUITest` automation.
  </action>
</task>

</tasks>

<verification>
- **Benchmark Results**: Run performance suite and verify all metrics meet the SOTA targets.
- **CI/CD Pass**: Verify that the CI configuration is valid and can execute the full test suite autonomously.
</verification>
