---
phase: 38-swiftui-profit-command-center-rl-training-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [backend/routes/alpha_routes.py, backend/utils/time_utils.py, backend/app_context.py, backend/agents/rl_policy.py]
autonomous: true
requirements: [DATA-01, DATA-02, RL-01]
must_haves:
  truths:
    - "WebSocket /api/alpha/stream emits portfolio vs benchmark metrics in real-time"
    - "Split-Brain controller correctly routes heavy tasks to GPU and light tasks to CPU"
    - "Smart Money window accurately identifies 14:00 UTC regardless of local DST"
  artifacts:
    - path: "backend/routes/alpha_routes.py"
      provides: "WebSocket endpoint for Alpha-Stream"
    - path: "backend/app_context.py"
      provides: "Split-Brain routing logic"
  key_links:
    - from: "backend/routes/alpha_routes.py"
      to: "backend/quant_engine.py"
      via: "Portfolio metrics fetch"
---

<objective>
Establish the data and control backbone for the Profit Command Center. This includes real-time WebSocket streaming of Alpha metrics and the 'Split-Brain' task routing architecture.

Purpose: High-performance data delivery and optimal hardware utilization on M4 Pro.
Output: Real-time data stream and intelligent task router.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/38-swiftui-profit-command-center-rl-training-integration/38-CONTEXT.md
@backend/app_context.py
@backend/quant_engine.py
</context>

<tasks>

<task type="auto" effort="medium">
  <name>Task 1: [AG] WebSocket Alpha-Stream & Smart Money DST</name>
  <files>
    backend/routes/alpha_routes.py
    backend/utils/time_utils.py
    backend/server.py
  </files>
  <action>
    <activate_skill name="security-review" />
    1. Create `backend/utils/time_utils.py` with `is_smart_money_window()` anchored to 14:00 UTC (9:00 AM NY / 2:00 PM London). Use `zoneinfo` for DST-safe math.
    2. Create `backend/routes/alpha_routes.py` with a FastApi WebSocket endpoint `/api/alpha/stream`.
    3. Implement a broadcast loop that fetches `PortfolioMetrics` and benchmark (FTSE 100) prices every 500ms.
    4. Register the route in `backend/server.py`.
    USE: `zoneinfo` for timezone handling.
    AVOID: Hardcoded UTC offsets that don't account for DST.
  </action>
  <verify>python3 -c "import asyncio, websockets; asyncio.run(websockets.connect('ws://localhost:8000/api/alpha/stream'))"</verify>
  <done>WebSocket emits JSON with {portfolio_val, benchmark_val, alpha, timestamp} at ~2Hz.</done>
</task>

<task type="auto" effort="medium">
  <name>Task 2: [CLI] Unified Control Pass & Policy Scaling</name>
  <files>
    backend/app_context.py
    backend/agents/rl_policy.py
  </files>
  <action>
    <activate_skill name="python-reviewer" />
    1. Update `AppContext` in `backend/app_context.py` to include `SplitBrainController`.
    2. Implement `route_task(task_type: str)`: 
        - 'REASONING', 'TRAINING', 'POLICY' -> `VLLMMXEngine` (GPU).
        - 'COORDINATION', 'TOOL_USE', 'NEWS' -> `Ollama` (CPU).
    3. Update `RLPolicy` in `backend/agents/rl_policy.py` to scale output actions for LSE Leveraged ETFs (handle GBX precision vs GBP).
    4. Ensure policy output is clipped to valid trading ranges [0, 1] for weights.
    USE: MLX native clipping for efficiency.
  </action>
  <verify>pytest tests/backend/test_split_brain.py</verify>
  <done>Tasks are routed to correct engines; policy outputs are correctly scaled for GBX tickers.</done>
</task>

</tasks>

<success_criteria>
- WebSocket stream active and emitting Alpha metrics.
- Split-brain routing verified via logs (GPU vs CPU usage).
- Smart Money window logic passes DST-edge-case tests.
</success_criteria>

<output>
After completion, create `.planning/phases/38-swiftui-profit-command-center-rl-training-integration/38-01-SUMMARY.md`
</output>
