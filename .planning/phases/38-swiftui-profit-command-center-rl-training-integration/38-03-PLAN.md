---
phase: 38-swiftui-profit-command-center-rl-training-integration
plan: 03
type: execute
wave: 3
depends_on: [38-02]
files_modified: [Growin/Views/AlphaDashboardView.swift, Growin/Services/AlphaStreamViewModel.swift, Growin/Services/NotificationManager.swift, Growin/Views/DashboardView.swift]
autonomous: true
requirements: [UI-01, UI-02, UI-03]
must_haves:
  truths:
    - "Dashboard renders at 120Hz on ProMotion displays (verified via performance overlay)"
    - "Regime indicator changes color/state based on market volatility"
    - "Local macOS toast appears when a high-conviction rebalance is detected"
  artifacts:
    - path: "Growin/Views/AlphaDashboardView.swift"
      provides: "120Hz Canvas-based Alpha chart"
    - path: "Growin/Services/NotificationManager.swift"
      provides: "Fast-HITL toast integration"
  key_links:
    - from: "Growin/Services/AlphaStreamViewModel.swift"
      to: "/api/alpha/stream"
      via: "WebSocket connection"
---

<objective>
Implement the high-fidelity Profit Command Center UI in SwiftUI. This involves a 120Hz Alpha tracking dashboard and real-time market regime indicators.

Purpose: Professional-grade visualization of RL performance and market state.
Output: High-performance SwiftUI dashboard with integrated HITL notifications.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: [AG] 120Hz Dashboard with SwiftUI Canvas</name>
  <files>Growin/Views/AlphaDashboardView.swift, Growin/Services/AlphaStreamViewModel.swift</files>
  <action>
    - Create `AlphaStreamViewModel.swift` to connect to `ws://localhost:8000/api/alpha/stream`.
    - Implement a background actor to transform incoming Alpha metrics into screen coordinates (DisplayPoints).
    - Create `AlphaDashboardView.swift` using `TimelineView(.animation)` and `Canvas`.
    - Draw a smooth line chart for Growin Portfolio vs FTSE 100 benchmark.
    - Ensure `.drawingGroup()` is used for Metal-accelerated rendering.
  </action>
  <verify>
    <automated>Manual verification on M4 Pro: Check for smooth 120fps animation in 'Alpha Dashboard' view.</automated>
  </verify>
  <done>Smooth, real-time Alpha line chart updating at high frequency without UI lag.</done>
</task>

<task type="auto">
  <name>Task 2: [CLI] Visual Regime Indicator & Fast-HITL Toasts</name>
  <files>Growin/Services/NotificationManager.swift, Growin/Views/RegimeIndicatorView.swift, Growin/Views/DashboardView.swift</files>
  <action>
    - Create `RegimeIndicatorView.swift`: A visual component with neon-glow states for CALM (Green), DYNAMIC (Yellow), and CRISIS (Red).
    - Create `NotificationManager.swift` using `UNUserNotificationCenter` to request permissions and send local banners.
    - Wire the `AlphaStreamViewModel` to trigger a toast when a rebalance recommendation (>0.95 conviction) is received.
    - Integrate `AlphaDashboardView` and `RegimeIndicatorView` into the main `DashboardView.swift`.
  </action>
  <verify>
    <automated>Trigger a mock WebSocket message with conviction=0.99 and verify macOS banner appears.</automated>
  </verify>
  <done>Regime indicator visible on dashboard; macOS notifications working for rebalance signals.</done>
</task>

</tasks>

<success_criteria>
- 120Hz dashboard responsiveness (no frame drops).
- Real-time Alpha tracking vs Benchmark.
- Functional Fast-HITL rebalance toasts.
</success_criteria>

<output>
After completion, create `.planning/phases/38-swiftui-profit-command-center-rl-training-integration/38-03-SUMMARY.md`
</output>
