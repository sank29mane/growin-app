---
phase: 38-swiftui-profit-command-center-rl-training-integration
plan: 04
type: execute
wave: 4
depends_on: [38-03]
files_modified: [backend/agents/ppo_agent.py, backend/agents/rl_utils.py, tests/backend/test_ppo_training.py]
autonomous: true
requirements: [RL-02, RL-03]
must_haves:
  truths:
    - "PPO training loop successfully reduces loss over 100 epochs on synthetic data"
    - "Reward function correctly penalizes high turnover and high volatility"
    - "GAE successfully produces advantage estimates that correlate with future returns"
  artifacts:
    - path: "backend/agents/ppo_agent.py"
      provides: "Full PPO training orchestration"
    - path: "backend/agents/rl_utils.py"
      provides: "GAE and Financial Reward implementations"
  key_links:
    - from: "backend/agents/ppo_agent.py"
      to: "backend/agents/rl_policy.py"
      via: "Policy/Critic updates"
---

<objective>
Implement and harden the reinforcement learning training loop using Proximal Policy Optimization (PPO). This includes trajectory collection, advantage estimation via GAE, and a specialized financial reward function.

Purpose: Self-correcting trading intelligence for LSE Leveraged ETFs.
Output: Hardened RL training pipeline ready for production calibration.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/38-swiftui-profit-command-center-rl-training-integration/38-CONTEXT.md
@backend/agents/rl_policy.py
</context>

<tasks>

<task type="auto" effort="high">
  <name>Task 1: [AG] Trajectory Collection & PPO Training Loop</name>
  <files>
    backend/agents/ppo_agent.py
  </files>
  <action>
    <activate_skill name="python-performance-optimization" />
    1. Create `PPOAgent` in `backend/agents/ppo_agent.py`.
    2. Implement `TrajectoryBuffer`: In-memory storage for (states, actions, log_probs, rewards, values, masks).
    3. Implement the main `train_on_batch` method:
        - Compute surrogate loss (clipped).
        - Compute value loss (MSE).
        - Compute entropy bonus (for exploration).
        - Use `mx.grad` and `nn.optimizers.Adam` for parameter updates.
    4. Support batching and shuffling of trajectories.
    AVOID: Sequential loops for gradient updates.
    USE: MLX vectorized operations for loss computation and gradient application.
  </action>
  <verify>python3 backend/agents/ppo_agent.py (Ensure training loop runs without error).</verify>
  <done>PPO training loop operational on MLX; gradients flowing to both actor and critic.</done>
</task>

<task type="auto" effort="medium">
  <name>Task 2: [CLI] GAE & Reward Function Hardening</name>
  <files>
    backend/agents/rl_utils.py
    tests/backend/test_ppo_training.py
  </files>
  <action>
    <activate_skill name="tdd-guide" />
    1. Create `backend/agents/rl_utils.py`.
    2. Implement `compute_gae(rewards, values, masks, gamma=0.99, lam=0.95)`: Generalized Advantage Estimation.
    3. Implement `financial_reward(delta_alpha, turnover, variance, tc_penalty=0.0005)`:
        - Reward = Delta Alpha - (Transaction Cost + Volatility Tax).
        - Volatility Tax = 0.5 * variance.
    4. Create `tests/backend/test_ppo_training.py` with a synthetic environment to verify that the agent can learn to maximize "Alpha" while minimizing "Volatility".
    USE: `pytest` with parameterized tests for edge cases.
  </action>
  <verify>pytest tests/backend/test_ppo_training.py</verify>
  <done>Reward logic verified; GAE accurately calculates advantages; training converges on synthetic tasks.</done>
</task>

</tasks>

<success_criteria>
- PPO training loop completes 100 iterations on synthetic data in < 60s.
- Reward function correctly weights Alpha vs Volatility/TC.
- Advantages calculated via GAE show positive correlation with future rewards.
</success_criteria>

<output>
After completion, create `.planning/phases/38-swiftui-profit-command-center-rl-training-integration/38-04-SUMMARY.md`
</output>
