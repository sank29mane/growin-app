---
phase: 38-swiftui-profit-command-center-rl-training-integration
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified: [backend/utils/clara_codec.py, backend/agents/clara_agent.py, backend/agents/rl_state.py]
autonomous: true
requirements: [MEM-01, MEM-02]
must_haves:
  truths:
    - "CLaRa environment is initialized with MLX-optimized weights"
    - "Documents are compressed into latent vectors with 32x-64x ratio"
    - "RL State successfully fuses 32-dim semantic context from CLaRa"
  artifacts:
    - path: "backend/utils/clara_codec.py"
      provides: "Text-to-Latent compression"
    - path: "backend/agents/clara_agent.py"
      provides: "Latent Memory Bank & Projection Head"
---

<objective>
Implement Apple CLaRa (Continuous Latent Rationalization) as the 'Institutional Memory' for the Growin App. 
This wave focuses on setting up the MLX-based compression environment and fusing semantic research into the RL state.

Purpose: Ground RL policy in fundamental news and SEC filings semantics.
Output: Semantic-aware state vector (96-dim).
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/38-swiftui-profit-command-center-rl-training-integration/38-CONTEXT.md
@backend/agents/rl_state.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: [CLI] CLaRa Environment Setup</name>
  <files>backend/utils/clara_codec.py</files>
  <action>
    - Install native MLX CLaRa bindings: `uv pip install ml-clara-mlx`.
    - Create `backend/utils/clara_codec.py` to manage the model lifecycle.
    - Implement `load_model()` to pull `apple/CLaRa-7B-Instruct` 4-bit weights.
    - Add a sanity check: `compress_text("Tesla reports record deliveries")` -> returns non-zero latent vector.
  </action>
  <verify>
    <automated>python3 backend/utils/clara_codec.py (Verification of weight loading and compression)</automated>
  </verify>
  <done>CLaRa environment is ready; weights loaded on ANE/GPU; compression verified.</done>
</task>

<task type="auto">
  <name>Task 2: [AG] ClaraAgent & Semantic Fusion</name>
  <files>backend/agents/clara_agent.py, backend/agents/rl_state.py</files>
  <action>
    - Create `backend/agents/clara_agent.py` implementing `ClaraAgent`.
    - Implement **Latent Projection Head** (MLX Linear layer) to squash 4096-dim latents into 32-dim policy context.
    - Implement `get_context_vector()` which pulls top-k semantic memories.
    - Update `backend/agents/rl_state.py`: 
        - Increase `state_dim` to 96.
        - Fuse 32-dim projection from `ClaraAgent` into the final state vector.
  </action>
  <verify>
    <automated>python3 backend/agents/rl_state.py (Assert shape is (96,))</automated>
  </verify>
  <done>RL State is now 96-dimensional and semantic-aware; Projection head squashes context effectively.</done>
</task>

</tasks>

<success_criteria>
- CLaRa weights loaded and inference verified on M4 Pro.
- Projection head reduces dimension without losing significant variance.
- RL State Vector shape = 96 (64 quant + 32 semantic).
</success_criteria>

<output>
After completion, create `.planning/phases/38-swiftui-profit-command-center-rl-training-integration/38-02-SUMMARY.md`
</output>
