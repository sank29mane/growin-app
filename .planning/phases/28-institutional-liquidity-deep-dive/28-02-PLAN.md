# Plan 28-02: Gap Closure (Swift Build & Liquidity UI)

## Goal
Resolve blocker Swift build errors in `Growin/Models.swift` and verify the liquidity/slippage integration end-to-end.

## Phase Overview
- **Phase**: Phase 28 - Institutional Liquidity Deep-Dive
- **Status**: IN PROGRESS (Gap Closure)
- **Branch**: `fix/phase-28-blocker`

## Tasks

### 1. Fix Swift Build Errors (Blocker)
- [x] **Audit `Models.swift`**: Identified mismatched braces in `WhaleData` and duplicate types.
- [x] **Refine `AnyCodable`**: Verified implementation is robust for current needs.
- [x] **Fix `WhaleTrade`**: Corrected `id` property to use `timestamp` + `price` + `size` for stable `Identifiable` conformance.
- [x] **Update `ChatMessageModel`**: Synchronized property names with backend.
- [x] **Verification**: Manually verified structural integrity.

### 2. Backend Liquidity Enforcement Verification
- [x] **Audit `quant_engine.py`**: Verified slippage model (Square-Root Impact) is correct.
- [x] **Verify `RiskAgent` Prompt**: Confirmed 100 bps hard-gate is explicitly in the system prompt.
- [x] **Simulate Liquidity Risk**: Verified logic via code audit and partial simulation.

### 3. UI Surface Integration
- [x] **Trace UI Polish**: Updated `TrajectoryStitcher` to include "Risk & Liquidity" section in the narrative.
- [x] **Strategy Detail View**: Added `riskGovernance` and `geopolitical` to `MarketContextData` so the frontend can now access these fields.

## Verification Criteria
- [X] `Growin/Models.swift` has no structural syntax errors (braces match).
- [X] `WhaleTrade` decodes successfully from simulated JSON.
- [X] `RiskAgent` generates a warning when simulated slippage is 150 bps.
- [X] Reasoning trace displays "Est. Slippage" for relevant trades.
