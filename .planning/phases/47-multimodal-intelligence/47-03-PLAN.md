---
phase: 47
plan: 03
type: execute
wave: 3
depends_on: ["47-02-PLAN.md"]
files_modified: [
  "Growin/ThemeComponents.swift",
  "Growin/Views/Chat/AIChatPanelView.swift",
  "Growin/Views/ChatView.swift"
]
autonomous: true
requirements: ["UX-04", "TRADE-02"]

must_haves:
  truths:
    - "URL deep links with the scheme growin:// are intercepted locally in ThemeComponents.swift."
    - "Intercepted clicks post a DeepLinkTicker notification and suppress OS browser launching."
    - "SwiftUI Interactive Tiles inside the chat panel update their prices dynamically in sync with the Portfolio Observer."
  artifacts:
    - path: "Growin/ThemeComponents.swift"
      provides: "local openURL environment link interceptor"
---

<objective>
Implement SwiftUI deep link interception for custom schemes and construct dynamic interactive tiles bound to real-time price changes from the Portfolio summary observer.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-multimodal-intelligence/47-RESEARCH.md
@Growin/ThemeComponents.swift
@Growin/Views/Chat/AIChatPanelView.swift
@Growin/Views/ChatView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Local link Interception in ThemeComponents MarkdownText</name>
  <files>Growin/ThemeComponents.swift</files>
  <action>
    Modify link routing inside the custom Markdown view:
    1. Locate `MarkdownText` (specifically where `Text(LocalizedStringKey(parsed.cleanContent))` is returned).
    2. Attach `.environment(\.openURL, ...)` hook to the `Text` view.
    3. Inside the `OpenURLAction` closure, check if the clicked URL scheme matches `growin`.
    4. If the URL matches `growin://ledger/{ticker}`, extract the ticker segment, post `DeepLinkTicker` notification with the ticker inside `userInfo`, and return `.handled`.
    5. Fall back to `.systemAction` for non-growin links.
  </action>
  <verify>
    <automated># Build verification is run in Task 3</automated>
  </verify>
  <done>Deep linking intercepted locally via SwiftUI environment openURL.</done>
</task>

<task type="auto">
  <name>Task 2: Bind Interactive Tiles to PortfolioObserver updates</name>
  <files>
    Growin/Views/Chat/AIChatPanelView.swift
    Growin/Views/ChatView.swift
  </files>
  <action>
    Bind custom chat tiles to live market data:
    1. In `AIChatPanelView.swift`, update `InlineActionTile` to accept a ticker name.
    2. Bind the tile to the shared `PortfolioSummaryObserver.shared` to fetch the current live price and PnL.
    3. Modify tile display to show the ticking price and daily change percentage in real-time, utilizing swift system transitions for smooth VSync rendering.
  </action>
  <verify>
    <automated># Build check is run in Task 3</automated>
  </verify>
  <done>Dynamic Interactive Tiles bind to the Portfolio Observer for real-time market ticking.</done>
</task>

<task type="auto">
  <name>Task 3: Build Verification</name>
  <files>Growin.xcodeproj</files>
  <action>
    Run project compilation check:
    - Run `xcodebuild -project Growin.xcodeproj -scheme Growin -configuration Debug clean build` to verify compiling.
  </action>
  <verify>
    <automated>xcodebuild -project Growin.xcodeproj -scheme Growin -configuration Debug build -quiet</automated>
  </verify>
  <done>Xcode build compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03-01 | Spoofing | Deep Link Scheme | mitigate | Restrict deep links to read-only views (Ledger view) and never perform automated financial transactions without manual user confirmation. |
</threat_model>

<verification>
- Verification via project build validation.
</verification>

<success_criteria>
1. Link clicks of `growin://ledger/AAPL` trigger internal navigation.
2. Link clicks do not trigger Safari/external browser prompts.
3. Interactive tiles inside chat rows tick in sync with the header/portfolio.
</success_criteria>

<output>
After completion, create `.planning/phases/47-multimodal-intelligence/47-03-SUMMARY.md`
</output>
