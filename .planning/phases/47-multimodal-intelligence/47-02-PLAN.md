---
phase: 47
plan: 02
type: execute
wave: 2
depends_on: ["47-01-PLAN.md"]
files_modified: [
  "backend/agents/decision_agent.py",
  "backend/routes/chat_routes.py",
  "Growin/Models.swift",
  "Growin/AgentClient.swift",
  "Growin/ViewModels/ChatViewModel.swift",
  "Growin/Views/ChatView.swift"
]
autonomous: true
requirements: ["INTEL-03"]

must_haves:
  truths:
    - "DecisionAgent correctly inserts image placeholders using Gemma-4 dialog structures."
    - "SwiftUI client encodes selected images to base64 and posts them to the API."
    - "ChatView rendering shows uploaded images in the chat bubbles."
  artifacts:
    - path: "backend/agents/decision_agent.py"
      provides: "gemma-4 VLM prompting formatting"
---

<objective>
Implement SwiftUI image selection and upload, propagate base64 strings to the backend API, and configure DecisionAgent's prompt formatting to comply with Gemma-4 VLM specifications.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-multimodal-intelligence/47-RESEARCH.md
@backend/agents/decision_agent.py
@backend/routes/chat_routes.py
@Growin/AgentClient.swift
@Growin/ViewModels/ChatViewModel.swift
@Growin/Views/ChatView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update DecisionAgent for Gemma-4 Prompting & Thought Stripping</name>
  <files>backend/agents/decision_agent.py</files>
  <action>
    Modify `backend/agents/decision_agent.py` to support VLM prompting rules:
    1. Update `make_decision` and `make_decision_stream` to accept an optional list of base64 images.
    2. Convert Base64 strings to PIL Images.
    3. Modify prompt construction to place the `<|image|>` token *before* the query content inside the user's turn block.
    4. Strip thought/reasoning tags from message history before formatting to conserve tokens in multi-turn conversations if "Thinking" mode is active.
    5. Pass the PIL images list to `ChatMLX` invoke/stream calls in the `images` keyword argument.
  </action>
  <verify>
    <automated># Build and execution verified in SwiftUI Integration</automated>
  </verify>
  <done>DecisionAgent structures vision prompts according to Gemma-4 rules.</done>
</task>

<task type="auto">
  <name>Task 2: Route Image Payloads in Chat API & SwiftUI Models</name>
  <files>
    backend/routes/chat_routes.py
    Growin/Models.swift
    Growin/AgentClient.swift
  </files>
  <action>
    Modify API route and networking layer to transfer base64 images:
    1. In `backend/routes/chat_routes.py`, accept `request.images` in both `chat_message` and `stream_chat_generator` endpoints, forwarding them to the `OrchestratorAgent`.
    2. In `Growin/Models.swift`, add an optional `images: [String]?` list property to `ChatMessageModel` (with coding keys).
    3. In `Growin/AgentClient.swift`, add `images: [String]?` to `streamMessage` parameter list and include it in the POST request body payload.
  </action>
  <verify>
    <automated># Build verification is run in Task 4</automated>
  </verify>
  <done>Image payloads are successfully routed from the SwiftUI client to the backend chat router.</done>
</task>

<task type="auto">
  <name>Task 3: Add Image Picker & Preview UI in SwiftUI ChatView</name>
  <files>
    Growin/ViewModels/ChatViewModel.swift
    Growin/Views/ChatView.swift
  </files>
  <action>
    Modify SwiftUI client frontend to support image attachment:
    1. In `Growin/ViewModels/ChatViewModel.swift`, add a list of selected images `var selectedImages: [NSImage] = []` and state to track base64 outputs.
    2. Update `sendMessageAsync` to encode `selectedImages` into Base64 strings, pass them to `agentClient.streamMessage`, and append them to the local `ChatMessageModel`.
    3. In `Growin/Views/ChatView.swift` (inside the input section), add an image attachment button (`paperclip` or `photo` icon) that triggers a macOS file picker.
    4. Display a horizontal preview list of attached images above the text input field, including a "delete" button for each image.
    5. Update `ChatBubble` to render uploaded images inside user message rows.
  </action>
  <verify>
    <automated># Build check is run in Task 4</automated>
  </verify>
  <done>SwiftUI client contains a functional image selection, preview, and rendering UI.</done>
</task>

<task type="auto">
  <name>Task 4: Build Verification</name>
  <files>Growin.xcodeproj</files>
  <action>
    Verify the app builds correctly after client-side additions:
    - Run `xcodebuild -project Growin.xcodeproj -scheme Growin -configuration Debug clean build` to verify compiling.
  </action>
  <verify>
    <automated>xcodebuild -project Growin.xcodeproj -scheme Growin -configuration Debug build -quiet</automated>
  </verify>
  <done>Xcode build compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-02-01 | Information Disclosure | Image Storage | mitigate | Ensure base64 strings in database do not leak through general API endpoints; serialize securely and restrict file permissions. |
</threat_model>

<verification>
- Verification via compilation tests and user-acceptance validation of image upload.
</verification>

<success_criteria>
1. User can attach an image from their local machine.
2. The image preview list displays the thumbnail.
3. The prompt is sent to the backend with images array, and response generated correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/47-multimodal-intelligence/47-02-SUMMARY.md`
</output>
