---
phase: 47
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "backend/mlx_engine.py",
  "backend/mlx_langchain.py",
  "backend/app_context.py",
  "backend/chat_manager.py"
]
autonomous: true
requirements: ["INTEL-03"]

must_haves:
  truths:
    - "MLXInferenceEngine successfully loads VLM models using mlx-vlm."
    - "ChatMLX supports image extraction from generation kwargs and implements async streaming."
    - "ChatMessage Pydantic model and SQLite database schema store and retrieve base64 image strings."
  artifacts:
    - path: "backend/mlx_engine.py"
      provides: "mlx-vlm execution interface"
---

<objective>
Extend the core local MLX inference engine and database persistence layers to support Vision-Language Models (VLMs) and store base64-encoded image payloads.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-multimodal-intelligence/47-RESEARCH.md
@backend/mlx_engine.py
@backend/mlx_langchain.py
@backend/app_context.py
@backend/chat_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend MLXInferenceEngine to support mlx-vlm</name>
  <files>backend/mlx_engine.py</files>
  <action>
    Modify `backend/mlx_engine.py` to add VLM inference support:
    1. Import `load` as `vlm_load`, `generate` as `vlm_generate`, and `stream_generate` as `vlm_stream_generate` from `mlx_vlm`.
    2. Store the loaded VLM processor as `self.processor` in `load_model` (which returns `model` and `processor` for VLMs).
    3. Update `generate` and `generate_stream` to accept an optional `images: Optional[List[Any]] = None` parameter.
    4. If `self.processor` is initialized, perform inference/streaming using `mlx_vlm`'s corresponding functions, decoding base64 image strings into `PIL.Image` objects.
  </action>
  <verify>
    <automated># Tested via integration tests in Task 3</automated>
  </verify>
  <done>MLXInferenceEngine supports VLM loading and inference.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance ChatMLX with images routing and stream support</name>
  <files>backend/mlx_langchain.py</files>
  <action>
    Modify `backend/mlx_langchain.py` to bridge LangChain calls with VLM engine:
    1. Update `_agenerate` to extract `images` from `kwargs` and pass them to `engine.generate`.
    2. Implement `_astream` method in `ChatMLX` to support native token-by-token streaming, yielding `ChatGenerationChunk` instances. Pass `images` from `kwargs` to `engine.generate_stream`.
  </action>
  <verify>
    <automated># Tested via integration tests in Task 3</automated>
  </verify>
  <done>ChatMLX supports images routing and async streaming.</done>
</task>

<task type="auto">
  <name>Task 3: Extend Pydantic Models and SQLite Schema for base64 images</name>
  <files>
    backend/app_context.py
    backend/chat_manager.py
  </files>
  <action>
    Modify models and SQLite schemas to persist images:
    1. In `backend/app_context.py`, add `images: Optional[List[str]] = None` to the `ChatMessage` Pydantic model.
    2. In `backend/chat_manager.py`, add a migration step in `_init_schema` to dynamically add the `images` text column to the `messages` table if it is missing.
    3. Update `save_message` and `load_history` / `get_conversation_history` to save and load images as a JSON-serialized string list.
  </action>
  <verify>
    <automated>pytest tests/test_mlx_vlm_engine.py</automated>
  </verify>
  <done>Pydantic models and database storage support base64 image list serialization.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01-01 | Denial of Service | MLX VLM Loading | mitigate | Limit the input resolution/size of uploaded images to prevent Metal memory exhaustion. |
</threat_model>

<verification>
- Verification via unit/integration tests confirming VLM load/inference and SQLite images serialization.
</verification>

<success_criteria>
1. MLX VLM loader successfully reads and generates text from a dummy image.
2. SQLite message logs correctly persist and recover stringified base64 image arrays.
3. Tests run and pass successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/47-multimodal-intelligence/47-01-SUMMARY.md`
</output>
