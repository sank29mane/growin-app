---
phase: 39-strategy-hardening-code-purity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/lm_studio_client.py
  - backend/mlx_engine.py
  - backend/agents/base_agent.py
  - backend/agents/quant_agent.py
  - backend/agents/social_agent.py
  - backend/agents/coordinator_agent.py
  - backend/agents/whale_agent.py
  - backend/agents/decision_agent.py
  - backend/agents/math_generator_agent.py
  - backend/agents/__init__.py
  - backend/agents/social_swarm/__init__.py
  - backend/agents/social_swarm/reddit_agent.py
  - backend/agents/social_swarm/twitter_agent.py
  - backend/agents/forecasting_agent.py
  - backend/agents/research_agent.py
  - backend/agents/rl_state.py
  - backend/agents/calibration_agent.py
  - backend/agents/ppo_agent.py
autonomous: true
requirements: [PHASE-39-01, PHASE-39-02]
must_haves:
  truths:
    - "No relative imports starting with 'from .' or 'from ..' in backend source code"
    - "Legacy LM Studio client file is permanently removed"
    - "MLXInferenceEngine has explicit type hints for all parameters and return values"
  artifacts:
    - path: "backend/mlx_engine.py"
      provides: "Type-safe MLX inference"
    - path: "backend/agents/base_agent.py"
      provides: "Absolute imports"
  key_links:
    - from: "backend/agents/coordinator_agent.py"
      to: "backend/agents/base_agent.py"
      via: "absolute import"
---

<objective>
Unify the backend architectural patterns by converting all relative imports to absolute paths, removing legacy AI adapters, and hardening MLX type safety for the M4 Pro inference engine.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/mlx_engine.py
@backend/agents/base_agent.py
@backend/lm_studio_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Absolute Imports Migration & Legacy Removal</name>
  <read_first>
    - backend/agents/ (scan all .py files for "from .")
    - backend/lm_studio_client.py
  </read_first>
  <files>
    backend/agents/**/*.py
    backend/lm_studio_client.py
  </files>
  <action>
    1. Scan all files in `backend/agents/` and its subdirectories.
    2. Convert all relative imports (e.g., `from .base_agent import ...`, `from ..data_models import ...`) to absolute imports (e.g., `from backend.agents.base_agent import ...`, `from backend.data_models import ...`).
    3. Remove `backend/lm_studio_client.py`.
    4. Remove any remaining OpenAI or LM Studio specific adapter code or fallbacks in `backend/agents/llm_factory.py` (if it exists) or `backend/mlx_engine.py`.
    5. Update `backend/agents/__init__.py` to use absolute imports for all exported agents.
  </action>
  <acceptance_criteria>
    - `grep -r "from \." backend/agents/` returns zero results (excluding comments).
    - `backend/lm_studio_client.py` is deleted.
    - All tests in `backend/tests/` pass with the new import structure.
  </acceptance_criteria>
  <verify>
    <automated>grep -r "from \." backend/agents/ --exclude-dir=__pycache__ --exclude=*.pyc | wc -l | grep "^0$" && ls backend/lm_studio_client.py 2>&1 | grep "No such file or directory"</automated>
  </verify>
  <done>Absolute imports implemented, legacy debt removed.</done>
</task>

<task type="auto">
  <name>Task 2: MLX Type Hardening & Optimization</name>
  <read_first>
    - backend/mlx_engine.py
    - backend/agents/ppo_agent.py
  </read_first>
  <files>
    backend/mlx_engine.py
    backend/agents/ppo_agent.py
  </files>
  <action>
    1. In `backend/mlx_engine.py`, add explicit type annotations for all methods in `MLXInferenceEngine`.
    2. Ensure `mlx_lm` and `mlx.core` types are correctly used (e.g., `mx.array` instead of `Any` where possible).
    3. Implement a strict check for Apple Silicon in `MLXInferenceEngine.__init__` and log a warning if not on `darwin`.
    4. In `backend/agents/ppo_agent.py`, add type hints to `PPOAgent` and `TrajectoryBuffer` methods.
    5. Hardwire the 8-bit AFFINE quantization check in `load_model` to verify the model configuration explicitly before loading.
  </action>
  <acceptance_criteria>
    - `mypy backend/mlx_engine.py` passes (or shows no new errors).
    - `MLXInferenceEngine.generate` has typed `max_tokens: int`, `temperature: float`, etc.
    - `PPOAgent.select_action` returns a typed tuple `Tuple[mx.array, mx.array, mx.array]`.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "from backend.mlx_engine import MLXInferenceEngine; import inspect; sig = inspect.signature(MLXInferenceEngine.generate); print(sig.parameters['max_tokens'].annotation)" | grep "int"</automated>
  </verify>
  <done>MLX components are type-safe and optimized for M4 Pro.</done>
</task>

</tasks>

<success_criteria>
- Backend source code uses 100% absolute imports.
- Legacy AI adapters are removed.
- MLX engine and PPO agent have robust type definitions.
</success_criteria>

<output>
After completion, create `.planning/phases/39-strategy-hardening-code-purity/39-01-SUMMARY.md`
</output>
