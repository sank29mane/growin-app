---
phase: 39-strategy-hardening-code-purity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/lm_studio_client.py
  - backend/mlx_engine.py
  - backend/agents/base_agent.py
  - backend/agents/quant_agent.py
  - backend/agents/coordinator_agent.py
  - backend/agents/decision_agent.py
  - backend/agents/rl_state.py
  - backend/agents/ppo_agent.py
  - backend/agents/__init__.py
autonomous: true
requirements: [PHASE-39-01, PHASE-39-02]
must_haves:
  truths:
    - "Core agents use absolute imports (e.g., 'from backend.agents.base_agent')"
    - "Legacy LM Studio client file is permanently removed"
    - "MLXInferenceEngine has explicit type hints and zero-copy memory stability check"
  artifacts:
    - path: "backend/mlx_engine.py"
      provides: "Type-safe MLX inference with memory tracking"
    - path: "backend/agents/base_agent.py"
      provides: "Absolute imports"
  key_links:
    - from: "backend/agents/coordinator_agent.py"
      to: "backend/agents/base_agent.py"
      via: "absolute import"
---

<objective>
Harden the core backend engine and primary agents by migrating to absolute imports, removing legacy adapters, and implementing MLX type safety with memory stability monitoring.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/mlx_engine.py
@backend/agents/base_agent.py
@backend/lm_studio_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core Absolute Imports & Legacy Removal</name>
  <read_first>
    - backend/agents/ (select core .py files)
    - backend/lm_studio_client.py
  </read_first>
  <files>
    backend/agents/base_agent.py
    backend/agents/coordinator_agent.py
    backend/agents/decision_agent.py
    backend/agents/ppo_agent.py
    backend/agents/rl_state.py
    backend/agents/quant_agent.py
    backend/agents/__init__.py
    backend/lm_studio_client.py
  </files>
  <action>
    1. Update the listed files to use absolute imports instead of relative ones (e.g., replace `from .base_agent` with `from backend.agents.base_agent`).
    2. Ensure `backend/agents/__init__.py` uses absolute imports for all exposed classes.
    3. Remove `backend/lm_studio_client.py`.
    4. Remove legacy adapter code in `backend/mlx_engine.py` that still references LM Studio or OpenAI-style endpoints if no longer needed.
  </action>
  <acceptance_criteria>
    - Listed files no longer contain relative imports starting with `from .`.
    - `backend/lm_studio_client.py` is deleted.
    - All core agents can be imported via absolute paths.
  </acceptance_criteria>
  <verify>
    <automated>grep "from \." backend/agents/base_agent.py backend/agents/coordinator_agent.py backend/agents/decision_agent.py backend/agents/ppo_agent.py backend/agents/rl_state.py backend/agents/quant_agent.py backend/agents/__init__.py | wc -l | grep "^0$" && ls backend/lm_studio_client.py 2>&1 | grep "No such file or directory"</automated>
  </verify>
  <done>Core absolute imports implemented, legacy debt removed.</done>
</task>

<task type="auto">
  <name>Task 2: MLX Type Hardening & Memory Stability</name>
  <read_first>
    - backend/mlx_engine.py
    - backend/agents/ppo_agent.py
  </read_first>
  <files>
    backend/mlx_engine.py
    backend/agents/ppo_agent.py
  </files>
  <action>
    1. In `backend/mlx_engine.py`, add explicit type annotations for all methods in `MLXInferenceEngine`.
    2. Implement a `check_memory_usage()` method in `MLXInferenceEngine` that logs `mlx.core.metal.get_peak_memory()` and `mlx.core.metal.get_active_memory()`.
    3. Ensure `MLXInferenceEngine.generate` logs memory delta before and after token generation to verify zero-copy stability.
    4. In `backend/agents/ppo_agent.py`, add type hints to `PPOAgent` and `TrajectoryBuffer` methods.
    5. Hardwire the 8-bit AFFINE quantization check in `load_model` to verify the model configuration explicitly before loading.
  </action>
  <acceptance_criteria>
    - `MLXInferenceEngine.generate` has typed `max_tokens: int`, `temperature: float`, etc.
    - Memory usage is logged during inference with zero-copy confirmation (stable memory delta).
    - `PPOAgent.select_action` returns a typed tuple.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "from backend.mlx_engine import MLXInferenceEngine; import inspect; sig = inspect.signature(MLXInferenceEngine.generate); print(sig.parameters['max_tokens'].annotation)" | grep "int"</automated>
  </verify>
  <done>MLX components are type-safe and memory usage is tracked for zero-copy stability.</done>
</task>

</tasks>

<success_criteria>
- Core backend source code uses absolute imports.
- Legacy AI adapters are removed.
- MLX engine has robust type definitions and verifies zero-copy memory stability.
</success_criteria>

<output>
After completion, create `.planning/phases/39-strategy-hardening-code-purity/39-01-SUMMARY.md`
</output>
