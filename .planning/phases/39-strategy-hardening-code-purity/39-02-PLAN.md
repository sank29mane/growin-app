---
phase: 39-strategy-hardening-code-purity
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - scripts/ppo_grid_search.py
  - backend/agents/ppo_agent.py
  - scripts/generate_lse_mappings.py
  - data/lse_leveraged_etfs.json
autonomous: true
requirements: [PHASE-39-03, PHASE-39-04]
must_haves:
  truths:
    - "PPO agent loss converges during backtest simulations"
    - "LSE ETP mappings accurately reflect 3x/5x leverage factors for 3QQQ and NVD3"
    - "Hyperparameter grid search generates a readable ranking of performance results"
  artifacts:
    - path: "scripts/ppo_grid_search.py"
      provides: "Automated RL optimization"
    - path: "data/etfs/lse_leveraged.json"
      provides: "Accurate leverage metadata"
  key_links:
    - from: "scripts/ppo_grid_search.py"
      to: "backend/agents/ppo_agent.py"
      via: "hyperparameter injection"
---

<objective>
Optimize the RL-driven rebalancing policy for LSE Leveraged ETFs by performing a hyperparameter grid search and calibrating financial rewards to account for Volatility Tax and institutional rebalance windows.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/agents/ppo_agent.py
@scripts/generate_lse_mappings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: PPO Grid Search & Reward Calibration</name>
  <read_first>
    - backend/agents/ppo_agent.py
    - backend/agents/rl_state.py
  </read_first>
  <files>
    scripts/ppo_grid_search.py
    backend/agents/ppo_agent.py
  </files>
  <action>
    1. Create `scripts/ppo_grid_search.py`.
    2. Implement a grid search over: `clip_epsilon` [0.1, 0.2], `entropy_coef` [0.001, 0.01], and `lr` [3e-4, 1e-4].
    3. Update `PPOAgent.loss_fn` to include a configurable `reward_scaling` factor.
    4. Implement "Financial Reward Calibration": Modify the reward calculation in the simulation environment (or within a mock env in the grid search script) to penalize "Volatility Tax" (high variance in rebalances) and reward timing aligned with the 2PM GMT window.
    5. Save results (mean reward, final loss) to `backend/data/ppo_results.json`.
  </action>
  <acceptance_criteria>
    - `scripts/ppo_grid_search.py` runs and evaluates at least 4 parameter combinations.
    - Resulting `ppo_results.json` contains a ranked list of hyperparameters.
    - Reward function explicitly calculates a penalty based on `mx.std(actions)`.
  </acceptance_criteria>
  <verify>
    <automated>python3 scripts/ppo_grid_search.py --smoke-test && ls backend/data/ppo_results.json</automated>
  </verify>
  <done>PPO hyperparameters optimized and rewards calibrated for financial stability.</done>
</task>

<task type="auto">
  <name>Task 2: LSE ETP Data Validation & Mapping</name>
  <read_first>
    - scripts/generate_lse_mappings.py
    - data/lse_leveraged_etfs.json
  </read_first>
  <files>
    data/etfs/lse_leveraged.json
    scripts/generate_lse_mappings.py
  </files>
  <action>
    1. Refine `scripts/generate_lse_mappings.py` to ensure it correctly parses "3x", "5x" from both `longName` and `ticker` suffix/prefix.
    2. Add `3QQQ.L`, `NVD3.L`, and `5LUK.L` explicitly to `data/lse_leveraged_etfs.json` if missing.
    3. Run `python3 scripts/generate_lse_mappings.py`.
    4. Manually verify `data/etfs/lse_leveraged.json` for accuracy on the key tickers (NVD3 should be 3.0 leverage).
  </action>
  <acceptance_criteria>
    - `data/etfs/lse_leveraged.json` exists and contains correct entries for NVD3 and 3QQQ.
    - `leverage` field in the JSON is a float matching the product spec (e.g., 3.0 for 3x).
  </acceptance_criteria>
  <verify>
    <automated>grep -A 5 "NVD3" data/etfs/lse_leveraged.json | grep "\"leverage\": 3.0"</automated>
  </verify>
  <done>LSE ETP data is accurate and ready for RL state fabrication.</done>
</task>

</tasks>

<success_criteria>
- PPO agent trained with optimal hyperparameters determined by grid search.
- Financial rewards calibrated to maximize Alpha while minimizing Volatility Tax.
- LSE ETP metadata is 100% accurate for 3x/5x products.
</success_criteria>

<output>
After completion, create `.planning/phases/39-strategy-hardening-code-purity/39-02-SUMMARY.md`
</output>
