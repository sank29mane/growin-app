---
phase: 39-strategy-hardening-code-purity
plan: 03
type: execute
wave: 3
depends_on: ["39-02"]
files_modified:
  - backend/routes/alpha_routes.py
  - backend/agents/ppo_agent.py
  - backend/tests/test_ws_training.py
autonomous: true
requirements: [PHASE-39-05]
must_haves:
  truths:
    - "WebSocket training stream provides real-time training metrics (loss, reward, entropy)"
    - "Training stream includes a Brain Stability metric based on policy variance"
    - "WebSocket connection handles disconnects and reconnects gracefully"
  artifacts:
    - path: "backend/routes/alpha_routes.py"
      provides: "WebSocket training endpoint"
    - path: "backend/tests/test_ws_training.py"
      provides: "Stream format verification"
  key_links:
    - from: "backend/routes/alpha_routes.py"
      to: "backend/agents/ppo_agent.py"
      via: "training callback / metrics fetch"
---

<objective>
Enable real-time RL brain stability monitoring in SwiftUI by implementing a WebSocket training stream that broadcasts PPO performance metrics and policy update stability.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/routes/alpha_routes.py
@backend/agents/ppo_agent.py
</context>

<tasks>

<task type="auto" effort="high">
  <name>Task 1: [AG] Training Metrics WebSocket Stream & Resilience</name>
  <files>
    backend/routes/alpha_routes.py
    backend/agents/ppo_agent.py
  </files>
  <action>
    <activate_skill name="security-review" />
    1. Update `backend/routes/alpha_routes.py` to include a new WebSocket endpoint: `/api/alpha/training-stream`.
    2. Implement a global metrics collector or event system (e.g., a simple async Queue) that `PPOAgent` can push training logs into after each batch update.
    3. Modify `PPOAgent.train_on_batch` to yield or return more detailed metrics (loss, entropy, value_loss, mean_reward).
    4. Ensure the WebSocket broadcast includes `timestamp`, `epoch`, `loss`, `reward`, and `entropy`.
    5. Implementation of "Graceful Disconnects": Add error handling in the WebSocket loop to catch `WebSocketDisconnect` exceptions and ensure the training loop continues even if the client disconnects.
    AVOID: Blocking the main training loop with slow WebSocket I/O.
    USE: Asyncio queues and non-blocking broadcasts.
  </action>
  <verify>python3 -c "from fastapi.testclient import TestClient; from backend.main import app; client = TestClient(app); with client.websocket_connect('/api/alpha/training-stream') as websocket: print('WS Connection OK')"</verify>
  <done>WebSocket `/api/alpha/training-stream` correctly broadcasts JSON payloads during training cycles and is resilient to client disconnection.</done>
</task>

<task type="auto" effort="medium">
  <name>Task 2: [AG] Brain Stability Monitoring Metric</name>
  <files>
    backend/agents/ppo_agent.py
    backend/routes/alpha_routes.py
    backend/tests/test_ws_training.py
  </files>
  <action>
    <activate_skill name="python-performance-optimization" />
    1. Define "Brain Stability" metric: Calculate the running variance of `policy` parameter updates or the KL divergence between successive policy distributions.
    2. Implement this metric in `PPOAgent` and add it to the WebSocket payload as `stability_score`.
    3. Create `backend/tests/test_ws_training.py` using `FastAPI.testclient` (WebSocket support) to verify that the `stability_score` is present and within expected bounds (0.0 to 1.0).
    4. Document the WebSocket JSON contract for the SwiftUI frontend team (mock artifact).
  </action>
  <verify>pytest backend/tests/test_ws_training.py</verify>
  <done>`stability_score` is present in the training stream and verified via automated tests.</done>
</task>

</tasks>

<success_criteria>
- SwiftUI dashboard can visualize RL convergence via the WebSocket stream.
- Training stability is measurable and observable in real-time.
- Minimal latency ( < 100ms) between training update and dashboard notification.
</success_criteria>

<output>
After completion, create `.planning/phases/39-strategy-hardening-code-purity/39-03-SUMMARY.md`
</output>
