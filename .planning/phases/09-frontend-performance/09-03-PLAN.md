---
phase: 09-frontend-performance
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified: ["Growin/ThemeComponents.swift", "Growin/Views/ReasoningTraceView.swift"]
autonomous: true
requirements: [FE-04]
---

<objective>
Standardize the Palette design system and implement the Reasoning Trace UI.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Palette Component Library</name>
  <action>
    - Extract `AgentStatusBadge`, `FinancialMetricView`, and `PremiumButton` into `ThemeComponents.swift`.
    - Ensure all components strictly adhere to the Stitch color palette.
    - Document usage in a new `docs/PALETTE_SYSTEM.md`.
  </action>
</task>

<task type="auto">
  <name>Task 2: Reasoning Trace UI</name>
  <action>
    - Create `ReasoningTraceView.swift`.
    - Implement a vertical timeline view that consumes the SSE telemetry events.
    - Show real-time status transitions (e.g., from pulsing "Working" to a green "Ready" checkmark).
  </action>
</task>

</tasks>

<verification>
- Verify that the new components are used consistently across the app.
- Check that the Reasoning Trace UI updates in real-time during a chat session.
</verification>
