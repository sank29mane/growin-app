---
phase: 09-frontend-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["Growin/ThemeComponents.swift", "Growin/Views/PortfolioView.swift", "Growin/ContentView.swift"]
autonomous: true
requirements: [FE-01]
---

<objective>
Optimize the SwiftUI frontend for 120Hz smoothness by identifying and fixing performance bottlenecks.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Optimize Glassmorphic Rendering</name>
  <action>
    - Review `ThemeComponents.swift` for `GlassCard` and `MeshBackground` implementation.
    - Implement `drawingGroup()` for static background elements to offload rendering to Metal.
    - Reduce redundant `blur` and `opacity` layers that trigger excessive off-screen rendering.
  </action>
</task>

<task type="auto">
  <name>Task 2: Scroll Performance Hardening</name>
  <action>
    - Update `PortfolioView.swift` and `ChatView` to use more efficient lazy loading.
    - Flatten complex view hierarchies in list items.
    - Ensure animations are using the `.spring()` modifier with appropriate damping to avoid stutter.
  </action>
</task>

</tasks>

<verification>
- Visual inspection of the code for performance anti-patterns.
- Compile the app to ensure no regressions in layout.
</verification>
