---
phase: 37-rl-regime-backtest-lab
plan: 03
type: execute
wave: 3
depends_on: ["37-02"]
files_modified:
  - backend/agents/ppo_agent.py
  - scripts/npu_backtest_lab.py
autonomous: true
requirements: [RL-37-03, BACKTEST-37-03]
must_haves:
  truths:
    - "PPO action head successfully outputs target weights for LSE ETPs"
    - "Parallel backtester can run 1000+ simulations in < 10 seconds"
  artifacts:
    - path: "backend/agents/ppo_agent.py"
      provides: "MLX Actor-Critic architecture"
    - path: "scripts/npu_backtest_lab.py"
      provides: "Vectorized GPU backtesting lab"
---

<objective>
Implement the PPO action head in MLX and deploy a high-performance parallel backtesting lab on the M4 Pro NPU/GPU.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/agents/ppo_agent.py
</context>

<tasks>

<task type="auto" effort="high">
  <name>Task 1: [AG] PPO Action Head & Actor-Critic Architecture</name>
  <files>
    backend/agents/ppo_agent.py
  </files>
  <action>
    <activate_skill name="python-performance-optimization" />
    1. Build a 3-layer MLP in MLX as the "Actor" for target weight selection.
    2. Build a matching "Critic" head for state-value estimation.
    3. Output Space: `[-1.0, 1.0]` (Target Portfolio Weight) clipped to valid trading ranges.
    4. Reward function: Maximize Cumulative Alpha - (Transaction Cost + Volatility Tax).
    USE: MLX `nn.Module` for architecture definition.
  </action>
  <verify>python3 backend/agents/ppo_agent.py --test-policy (Verify non-zero weights and gradient flow)</verify>
  <done>PPO policy head implemented with Actor-Critic architecture on MLX.</done>
</task>

<task type="auto" effort="max">
  <name>Task 2: [CLI] Parallel Backtester & Regime Frequency Calibration</name>
  <files>
    scripts/npu_backtest_lab.py
  </files>
  <action>
    <activate_skill name="tdd-guide" />
    1. Develop `scripts/npu_backtest_lab.py` using MLX `mx.vmap` to vectorize the simulation loop.
    2. Parallelize 1,000 simulations over historical LSE Leveraged ETF data.
    3. Calibrate rebalancing frequency (5m, 15m, 30m) based on current Market Regime.
    4. Calculate Performance Metrics: Sharpe Ratio, Sortino, Max Drawdown, and "Volatility Tax" drag.
    USE: `mx.vmap` for zero-overhead parallelism on Apple Silicon.
    AVOID: Sequential for-loops in the backtest logic.
  </action>
  <verify>python3 scripts/npu_backtest_lab.py (Performance test: Verify 1000 iterations < 10s)</verify>
  <done>Parallel backtesting lab operational; optimal rebalance frequencies identified per regime.</done>
</task>

</tasks>

<success_criteria>
- Backtest suite correctly identifies "Volatility Tax" threshold for 3x ETPs.
- PPO policy learns to avoid over-trading during high-volatility regimes.
- Total memory usage remains stable during 1000-parallel simulations.
</success_criteria>

<output>
After completion, create `.planning/phases/37-rl-regime-backtest-lab/37-03-SUMMARY.md`
</output>
