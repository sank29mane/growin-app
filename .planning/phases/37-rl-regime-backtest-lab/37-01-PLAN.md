---
phase: 37-rl-regime-backtest-lab
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/vllm_mlx_engine.py
  - scripts/fetch_leveraged_etf_data.py
  - data/etfs/lse_leveraged.json
  - utils/currency_utils.py
  - backend/agents/rl_state.py
autonomous: true
requirements: [INFRA-37-01, DATA-37-01]
must_haves:
  truths:
    - "vllm-mlx server is operational and responding to queries"
    - "LSE Leveraged ETPs are accurately mapped with leverage factors"
    - "RL State successfully fuses JMCE and TTM-R2 metrics"
  artifacts:
    - path: "backend/vllm_mlx_engine.py"
      provides: "Native MLX inference management"
    - path: "data/etfs/lse_leveraged.json"
      provides: "Ticker-to-Leverage mapping"
---

<objective>
Deploy the native vllm-mlx inference engine and establish the data foundation for LSE Leveraged ETF trading.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" effort="high">
  <name>Task 1: [CLI] Deploy vllm-mlx Server & Engine</name>
  <files>
    backend/vllm_mlx_engine.py
  </files>
  <action>
    <activate_skill name="python-performance-optimization" />
    1. Install `vllm-mlx` via `uv`.
    2. Create `backend/vllm_mlx_engine.py` to manage the lifecycle of the Nemotron-3-Nano process.
    3. Configure PagedAttention block size and GPU memory fraction (~0.7).
    AVOID: OOM errors on M4 Pro by setting aggressive KV cache limits.
    USE: `subprocess` with health checks for process management.
  </action>
  <verify>python3 backend/vllm_mlx_engine.py --test-query "Hello"</verify>
  <done>vllm-mlx native server is operational and responding to test queries.</done>
</task>

<task type="auto" effort="medium">
  <name>Task 2: [CLI] LSE Leveraged ETF Scraper & Normalization</name>
  <files>
    scripts/fetch_leveraged_etf_data.py
    data/etfs/lse_leveraged.json
    utils/currency_utils.py
  </files>
  <action>
    <activate_skill name="python-reviewer" />
    1. Execute `scripts/fetch_leveraged_etf_data.py`.
    2. Build `data/etfs/lse_leveraged.json` with ticker mappings (e.g., TSL3 -> Tesla 3x Long).
    3. Implement GBX-to-GBP normalization in `utils/currency_utils.py`.
    USE: Standardized mapping format for leverage (-3x, 3x, 5x).
  </action>
  <verify>python3 scripts/fetch_leveraged_etf_data.py && cat data/etfs/lse_leveraged.json</verify>
  <done>LSE ETP data harvested and currency normalization logic implemented.</done>
</task>

<task type="auto" effort="medium">
  <name>Task 3: [AG] RL State Fusion Logic</name>
  <files>
    backend/agents/rl_state.py
  </files>
  <action>
    <activate_skill name="python-performance-optimization" />
    1. Complete `backend/agents/rl_state.py`.
    2. Integrate **2:00 PM GMT Smart Money** boolean indicator.
    3. Normalize JMCE Mu/Sigma vectors using MLX `mx.norm`.
    USE: MLX native vector operations for state normalization.
  </action>
  <verify>python3 backend/agents/rl_state.py (Verification of state shape and normalization)</verify>
  <done>RL State fabricator successfully fuses market data into normalized vectors.</done>
</task>

</tasks>

<success_criteria>
- vllm-mlx handles concurrent queries.
- LSE mapping is comprehensive for target ETPs.
- RL state vector is ready for policy training.
</success_criteria>

<output>
After completion, create `.planning/phases/37-rl-regime-backtest-lab/37-01-SUMMARY.md`
</output>
