---
phase: 37-rl-regime-backtest-lab
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - backend/agents/regime_agent.py
  - backend/agents/calibration_agent.py
autonomous: true
requirements: [RL-37-02, ML-37-02]
must_haves:
  truths:
    - "RegimeAgent accurately calculates the spectral radius from JMCE factors"
    - "Calibration loop updates LoRA adapters based on daily market error"
  artifacts:
    - path: "backend/agents/regime_agent.py"
      provides: "Volatility clustering via Eigenvalues"
    - path: "backend/agents/calibration_agent.py"
      provides: "Nightly LoRA recalibration"
---

<objective>
Implement predictive regime detection using JMCE eigenvalues and establish a nightly LoRA calibration loop to minimize forecasting error.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@backend/agents/regime_agent.py
</context>

<tasks>

<task type="auto" effort="high">
  <name>Task 1: [AG] JMCE Eigenvalue Integration & Volatility Clustering</name>
  <files>
    backend/agents/regime_agent.py
  </files>
  <action>
    <activate_skill name="python-performance-optimization" />
    1. Update `RegimeAgent` to ingest JMCE Cholesky factor `L`.
    2. Calculate $\Sigma = LL^T$ and extract the Spectral Radius (Largest Eigenvalue) using `mx.linalg.eigvalsh`.
    3. Define Regime Thresholds: `CALM` (Low Variance), `DYNAMIC` (Expansion), `CRISIS` (Contagion) based on historical quantile analysis of eigenvalues.
    USE: MLX native linear algebra for spectral decomposition.
  </action>
  <verify>python3 backend/agents/regime_agent.py (Verify regime switching on simulated JMCE input)</verify>
  <done>Regime detection correctly clusters market volatility using JMCE spectral analysis.</done>
</task>

<task type="auto" effort="high">
  <name>Task 2: [AG] Nightly Calibration (ReFT) & Feedback Loop</name>
  <files>
    backend/agents/calibration_agent.py
  </files>
  <action>
    <activate_skill name="python-reviewer" />
    1. Implement `backend/agents/calibration_agent.py`.
    2. Create a feedback loop: Compare `MarketContext.forecast` vs `PriceData.actual` at LSE close.
    3. Update LoRA adapters for TTM-R2 decoder head using `mlx.optimizers`.
    4. Implement "ReFT" (Representation Fine-Tuning) to adjust model hidden states for regime-specific forecasting.
    AVOID: Overfitting on a single day's noise.
    USE: EMA (Exponential Moving Average) weights for parameter updates.
  </action>
  <verify>python3 backend/agents/calibration_agent.py --mock-day (Verify loss reduction on mock market feedback)</verify>
  <done>Nightly calibration loop established; adapters are updated based on recent forecasting accuracy.</done>
</task>

</tasks>

<success_criteria>
- Regime agent successfully transitions between Calm/Crisis states.
- Calibration loop reduces forecast MSE over multiple mock iterations.
- ANE/GPU utilization remains efficient during nightly updates.
</success_criteria>

<output>
After completion, create `.planning/phases/37-rl-regime-backtest-lab/37-02-SUMMARY.md`
</output>
