---
phase: 42-model-performance-comparison
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [
  "Growin/Views/Chat/WelcomeView.swift",
  "Growin/Views/Chat/AIChatPanelView.swift",
  "Growin/Views/Chat/AccountPicker.swift",
  "backend/data_models.py",
  "backend/agents/decision_agent.py",
  "backend/agents/orchestrator_agent.py",
  "backend/routes/chat_routes.py",
  "Growin/Models.swift",
  "Growin/Views/RichMessageComponents.swift",
  "Growin/Views/Chat/EnhancedTypingIndicator.swift",
  "Growin/CoreModels.swift"
]
autonomous: true
requirements: [UX-01, UX-02, UX-05]

must_haves:
  truths:
    - "Welcome screen appears when chat is empty"
    - "Suggestion chips trigger new chat messages on tap"
    - "Account picker switches between All/ISA/Invest accounts"
    - "Structured quick actions appear as tappable buttons below AI messages"
    - "Typing indicator displays reasoning steps (e.g., 'Analyzing sentiment...')"
    - "Cash balance is visible in portfolio components"
  artifacts:
    - path: "Growin/Views/Chat/WelcomeView.swift"
      provides: "Empty state UI with suggestion chips"
    - path: "Growin/Views/Chat/AccountPicker.swift"
      provides: "Account type selection UI"
    - path: "Growin/Views/Chat/EnhancedTypingIndicator.swift"
      provides: "Animated feedback with reasoning steps"
    - path: "backend/agents/decision_agent.py"
      provides: "Logic for generating contextual quick actions"
  key_links:
    - from: "AIChatPanelView.swift"
      to: "WelcomeView.swift"
      via: "Conditional rendering (messages.isEmpty)"
    - from: "DecisionAgent.py"
      to: "Frontend"
      via: "JSON response with quick_actions array"
---

<objective>
Implement SOTA AI chat interface enhancements to improve user engagement, reduce friction in the empty state, and provide contextual navigation and data accuracy.

Purpose: Bring the Growin chat experience in line with 2024-2025 SOTA patterns (ChatGPT, Claude, Perplexity).
Output: A polished, macOS-native chat interface with high-fidelity components and improved data visibility.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/CHAT_ENHANCEMENT_PLAN.md

<interfaces>
From Growin/Models.swift:
```swift
struct ChatMessageModel: Codable, Identifiable, Equatable {
    let messageId: String
    let role: String
    let content: String
    // Add quickActions: [QuickAction]? here
}

struct QuickAction: Codable, Identifiable, Equatable {
    let id = UUID()
    let icon: String
    let label: String
    let prompt: String
}
```

From backend/data_models.py:
```python
class QuickAction(BaseModel):
    icon: str
    label: str
    prompt: str
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Wave 1: Welcome Screen & Suggestion Chips</name>
  <files>Growin/Views/Chat/WelcomeView.swift, Growin/Views/Chat/AIChatPanelView.swift</files>
  <action>
    - Create `WelcomeView.swift` following the design in `docs/CHAT_ENHANCEMENT_PLAN.md`.
    - Implement `SuggestionChip` with hover effects and Tahoe "Liquid Glass" materials (`.ultraThinMaterial`).
    - Define a standard set of 6 `SuggestionItem`s (Portfolio Overview, Tomorrow's Plays, etc.).
    - Update `AIChatPanelView.swift` to render `WelcomeView` when the message list is empty.
    - Ensure tapping a chip triggers the `sendMessage` function in the view model.
  </action>
  <verify>
    <automated>Check if WelcomeView.swift exists and AIChatPanelView.swift imports/uses it.</automated>
  </verify>
  <done>Welcome screen is visible on empty chat, chips are interactive and send prompts.</done>
</task>

<task type="auto">
  <name>Wave 2: Account Picker & Quick Actions</name>
  <files>Growin/Views/Chat/AccountPicker.swift, Growin/Views/Chat/AIChatPanelView.swift, backend/data_models.py, backend/agents/decision_agent.py, backend/agents/orchestrator_agent.py, backend/routes/chat_routes.py, Growin/Models.swift, Growin/Views/RichMessageComponents.swift</files>
  <action>
    - **Frontend (Account Picker)**: Create `AccountPicker.swift` (segmented control style) and place it above the message input in `AIChatPanelView`. Bind it to the `accountType` in the view model.
    - **Backend (Quick Actions)**:
      - Add `QuickAction` class to `backend/data_models.py`.
      - Implement `_get_quick_actions(self, context)` in `DecisionAgent` to return contextual suggestions based on ticker or portfolio state.
      - Update `OrchestratorAgent` and `chat_routes.py` to include `quick_actions` in the JSON response payload.
    - **Frontend (Quick Actions)**:
      - Update `ChatMessageModel` in `Growin/Models.swift` to include `quickActions: [QuickAction]?`.
      - Create `QuickActionButtons` component and integrate it into `RichMessageComponents.swift` (or directly in the chat bubble) to render tappable actions below AI responses.
  </action>
  <verify>
    <automated>Verify backend /api/chat/message returns 'quick_actions' field. Check AccountPicker rendering in SwiftUI.</automated>
  </verify>
  <done>Account picker switches state; AI messages show contextual action buttons.</done>
</task>

<task type="auto">
  <name>Wave 3: Enhanced Typing Indicator & Data Accuracy</name>
  <files>Growin/Views/Chat/EnhancedTypingIndicator.swift, Growin/Views/Chat/AIChatPanelView.swift, Growin/CoreModels.swift, Growin/Models.swift, Growin/Views/RichMessageComponents.swift</files>
  <action>
    - **Typing Indicator**: Create `EnhancedTypingIndicator.swift` with an animated brain icon and dynamic "reasoning steps" text (e.g., "Scanning news...", "Synthesizing strategy..."). Update `AIChatPanelView` to replace the basic indicator.
    - **Data Accuracy**:
      - Ensure `cash_balance` is consistently defined as a `CashBalance` struct in both `Growin/Models.swift` and `Growin/CoreModels.swift`.
      - Update `PortfolioSnapshotCard` in `RichMessageComponents.swift` to display "Cash Available" using the `cash_balance` from the portfolio data.
      - Verify backend `data_models.py` already provides `cash_balance` (it does) and ensure it's correctly mapped in the Swift `Decodable` implementations.
  </action>
  <verify>
    <automated>Check for EnhancedTypingIndicator usage in AIChatPanelView.swift. Verify cashBalance field usage in RichMessageComponents.swift.</automated>
  </verify>
  <done>Typing indicator is more informative; Portfolio cards show accurate cash balances.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> API | User-selected account type and prompts pass through here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Information Disclosure | Account Picker | mitigate | Ensure account filter only returns data for the specific account requested (ISA/Invest) |
| T-42-02 | Tampering | Suggestion Chips | mitigate | Validate that suggested prompts are safe to execute on the backend |
</threat_model>

<verification>
- Run SwiftUI Preview for `WelcomeView` and `AccountPicker`.
- Test chat flow: Empty state -> Tap Suggestion -> Receive response -> See Quick Actions.
- Switch account types and verify the `account_type` parameter in backend logs.
- Verify `Cash Available` appears in portfolio cards when querying "how is my portfolio".
</verification>

<success_criteria>
- 100% of tasks in waves 1-3 implemented and verified.
- UI follows macOS Tahoe "Liquid Glass" materials.
- Chat frictionless experience from empty state to actionable response.
</success_criteria>

<output>
After completion, create `.planning/phases/42-model-performance-comparison/42-04-SUMMARY.md`
</output>
