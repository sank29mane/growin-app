---
phase: 45
plan: 03
type: execute
wave: 3
depends_on: ["45-02"]
files_modified: [
  "Growin/ViewModels/StockChartViewModel.swift",
  "Growin/Views/StockChartView.swift"
]
autonomous: true
requirements: ["UX-05", "TRADE-01"]

must_haves:
  truths:
    - "High-frequency updates are synchronized via a CADisplayLink aligned timer loop."
    - "Chart values are throttled or buffered to prevent main thread blocking."
    - "App compiles successfully."
  artifacts:
    - path: "Growin/ViewModels/StockChartViewModel.swift"
      provides: "CADisplayLink-driven VSync rendering loop and buffering for prices"
---

<objective>
Implement ProMotion 120Hz display link and sub-16ms price tick buffering to ensure fluent layout updates and zero rendering drops on Apple ProMotion displays.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-sovereign-ux/45-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CADisplayLink VSync Controller to Chart ViewModel</name>
  <files>Growin/ViewModels/StockChartViewModel.swift</files>
  <action>
    Integrate `CADisplayLink` into the rendering cycle of `StockChartViewModel`:
    1. Create a display link controller or add private `CADisplayLink` support to `StockChartViewModel`.
    2. Buffering Incoming Ticks: Instead of immediately modifying `chartData` on every WebSocket message, write incoming ticks to a concurrent queue buffer.
    3. On VSync Tick: Dequeue the latest price points from the buffer and batch-update the view-bound properties (e.g., `chartData` or current prices) at the display's exact refresh rate (supporting up to 120fps).
  </action>
  <verify>
    <automated># Checked during build verification</automated>
  </verify>
  <done>CADisplayLink integration buffers price ticks and flushes them aligned to VSync.</done>
</task>

<task type="auto">
  <name>Task 2: Optimize Rendering Constraints in StockChartView</name>
  <files>Growin/Views/StockChartView.swift</files>
  <action>
    Modify `StockChartView.swift` to optimize render execution:
    1. Verify view layout boundaries are constrained properly to avoid layout thrashing during high-frequency draws.
    2. Downsample chart data inside the chart container if points exceed 1000 to maintain smooth 120Hz frame rates.
  </action>
  <verify>
    <automated># Checked during build verification</automated>
  </verify>
  <done>StockChartView is optimized for low rendering overhead at 120fps.</done>
</task>

<task type="auto">
  <name>Task 3: Build Verification</name>
  <files>Growin.xcodeproj</files>
  <action>
    Verify compile success:
    - Run `xcodebuild -project Growin.xcodeproj -scheme Growin -configuration Debug build -quiet`.
  </action>
  <verify>
    <automated>xcodebuild -project Growin.xcodeproj -scheme Growin -configuration Debug build -quiet</automated>
  </verify>
  <done>Xcode target compiles successfully.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03-01 | Denial of Service | Main UI Thread | mitigate | Dynamic buffering limits maximum VSync callback work to <5ms to prevent thread starvation. |
</threat_model>

<verification>
- Verification via Xcode build command.
</verification>

<success_criteria>
1. CADisplayLink updates aligned to VSync.
2. High-frequency price ticks buffered.
3. Build completes successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/45-sovereign-ux/45-03-SUMMARY.md`
</output>
