---
phase: 45
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified: [
  "Growin/GrowinApp.swift",
  "Growin/Views/MainTabView.swift"
]
autonomous: true
requirements: ["UX-02", "UX-03"]

must_haves:
  truths:
    - "Separate Window scenes exist for charts and agent console in GrowinApp.swift."
    - "A workspace trigger launches the window sets (trading window layouts) simultaneously using openWindow."
    - "The app compiles successfully."
  artifacts:
    - path: "Growin/GrowinApp.swift"
      provides: "Multi-window scene configuration"
    - path: "Growin/Views/MainTabView.swift"
      provides: "Trigger to launch the full trading workspace"
---

<objective>
Configure Stage Manager 2.0 window sets by registering dedicated window groups for trading views and adding a workspace action trigger to launch them concurrently.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-sovereign-ux/45-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Window Scenes in GrowinApp</name>
  <files>Growin/GrowinApp.swift</files>
  <action>
    Modify `Growin/GrowinApp.swift` to add secondary window scenes:
    1. Register a `WindowGroup(id: "live-charts")` that loads `StockChartView()` or `ChartsView()`.
    2. Register a `WindowGroup(id: "agent-console")` that loads `IntelligentConsoleView()`.
    3. Ensure custom window styles (`.windowStyle(.hiddenTitleBar)`) and background materials are applied to make them blend in Tahoe style.
  </action>
  <verify>
    <automated># Checked during build verification</automated>
  </verify>
  <done>Window groups for charts and console are declared in GrowinApp.swift.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Workspace Set Launcher</name>
  <files>Growin/Views/MainTabView.swift</files>
  <action>
    Add a button to launch the full Stage Manager layout:
    1. Add `@Environment(\.openWindow) private var openWindow` to `MainTabView.swift`.
    2. Create a "Workspace Set" control button (or menu item) in the top action bar of `MainTabView`.
    3. On click, call `openWindow(id: "live-charts")` and `openWindow(id: "agent-console")` to trigger macOS Stage Manager window clustering.
  </action>
  <verify>
    <automated># Checked during build verification</automated>
  </verify>
  <done>Workspace set action button is present and triggers multi-window launch.</done>
</task>

<task type="auto">
  <name>Task 3: Build Verification</name>
  <files>Growin.xcodeproj</files>
  <action>
    Compile the Xcode target to verify multi-scene syntax and environment bindings:
    - Run `xcodebuild -project Growin.xcodeproj -scheme Growin -configuration Debug build -quiet`.
  </action>
  <verify>
    <automated>xcodebuild -project Growin.xcodeproj -scheme Growin -configuration Debug build -quiet</automated>
  </verify>
  <done>Multi-window app target builds successfully without errors.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02-01 | Tampering | Window Routing | mitigate | Validate all inputs or identifiers for window routing to prevent shell launching from window requests. |
</threat_model>

<verification>
- Verification via Xcode build command.
</verification>

<success_criteria>
1. `WindowGroup` IDs configured.
2. `openWindow` action triggers compilation passes.
3. App targets compile successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/45-sovereign-ux/45-02-SUMMARY.md`
</output>
