---
phase: 45
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "Growin/ThemeComponents.swift",
  "Growin/Views/DashboardView.swift"
]
autonomous: true
requirements: ["UX-01", "UX-05"]

must_haves:
  truths:
    - "UI components utilize 0px corner radiuses instead of rounded corners."
    - "Background components leverage macOS thin/ultraThin material backgrounds (Tahoe Liquid Glass)."
    - "App builds successfully without compiler warnings or errors."
  artifacts:
    - path: "Growin/ThemeComponents.swift"
      provides: "0px corner primitives and Tahoe material cards"
---

<objective>
Scaffold the SwiftUI 17+ 0px Ledger UI styles. Redesign core layout elements (GlassCard, PremiumButton) to follow 0px sharp rectangular borders and Tahoe translucent material layers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-sovereign-ux/45-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign Primitives in ThemeComponents</name>
  <files>Growin/ThemeComponents.swift</files>
  <action>
    Modify `Growin/ThemeComponents.swift` to align with the 0px brutalist design spec:
    1. Update `PremiumTypography` font designs to use native sans/monospaced instead of `.rounded`.
    2. Redesign `GlassCard` to use a corner radius of `0` (or replace `RoundedRectangle` with `Rectangle`).
    3. Modify borders and background fills in `GlassCard` to use thin/ultraThin materials with fine solid borders (`lineWidth: 0.5`).
    4. Redesign `PremiumButton` and other helper components to strictly enforce `0` corner radius and flat/ledgier aesthetics.
  </action>
  <verify>
    <automated># Compilation check is run in Task 3</automated>
  </verify>
  <done>Core theme primitives in ThemeComponents use 0px corners and macOS materials.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Dashboard View Layout</name>
  <files>Growin/Views/DashboardView.swift</files>
  <action>
    Apply the updated 0px design language to the main dashboard container:
    1. Ensure containers, list cards, and tables in `DashboardView.swift` leverage the updated `GlassCard` (with 0px corner radius).
    2. Adjust spacing and alignment to fit the brutalist editorial aesthetic (high-density layout).
  </action>
  <verify>
    <automated># Checked during full compilation</automated>
  </verify>
  <done>Dashboard view successfully rendered using the 0px layout engine.</done>
</task>

<task type="auto">
  <name>Task 3: Build Verification</name>
  <files>Growin.xcodeproj</files>
  <action>
    Run Xcode command-line build tools to verify the app compiles cleanly:
    - Run `xcodebuild -project Growin.xcodeproj -scheme Growin -configuration Debug clean build` (or similar command) to ensure zero compilation errors.
  </action>
  <verify>
    <automated>xcodebuild -project Growin.xcodeproj -scheme Growin -configuration Debug build -quiet</automated>
  </verify>
  <done>Xcode build compiles successfully without errors.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01-01 | Tampering | UI Components | mitigate | Ensure no dynamic component injection; all styles are compile-time checked swift structs. |
</threat_model>

<verification>
- Verification via Xcode build command.
</verification>

<success_criteria>
1. All premium cards and buttons rendered with 0px sharp corners.
2. Background containers apply thin material window blends.
3. macOS build compiles successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/45-sovereign-ux/45-01-SUMMARY.md`
</output>
