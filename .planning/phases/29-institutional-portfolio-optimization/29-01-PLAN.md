# Plan: Phase 29 - Institutional Portfolio Optimization (Mean-Variance)

Implement an institutional-grade portfolio optimization engine using Neural JMCE on Apple Silicon M4 NPU, integrated with a deterministic CVaR risk engine and asynchronous monitoring.

## 🟢 Task 1: Regime-Aware Data Layer
Implement tools to fetch macro signals and parallel historical time-series for portfolio-wide optimization.

- **Action**: Create `backend/utils/regime_fetcher.py` to fetch VIX, 10Y Treasury Yield, and Inflation (CPI) data.
- **Action**: Update `backend/data_fabricator.py` to include a `portfolio_optimization` intent that fetches parallel 180-day historical series for all tickers in the portfolio.
- **Verification**: `PYTHONPATH=. uv run python3 -c "import asyncio; from backend.utils.regime_fetcher import RegimeFetcher; print(asyncio.run(RegimeFetcher().fetch_signals()))"` must return VIX and 10Y data.

## 🟢 Task 2: MLX Neural JMCE Implementation
Build the core Transformer-based Joint Mean-Covariance Estimator optimized for M4 NPU.

- **Action**: Create `backend/utils/jmce_model.py` using Apple MLX. Implement the Transformer backbone and Cholesky heads for covariance estimation.
- **Action**: Implement `_build_cholesky` to ensure predicted covariance matrices are Positive Definite.
- **Verification**: Run a synthetic forward pass test: `PYTHONPATH=. uv run python3 tests/backend/test_jmce_forward.py`. Must utilize NPU (check via `asitop` or similar if available).

## 🟢 Task 3: Deterministic RiskEngine (CVaR)
Implement the "Source of Truth" for risk metrics to prevent LLM hallucinations.

- **Action**: Create `backend/utils/risk_engine.py`. Implement `calculate_cvar_95` using NumPy's historical/empirical approach.
- **Action**: Ensure the engine accepts raw return series and outputs a precision-safe `Decimal` risk value.
- **Verification**: Create `tests/backend/test_risk_engine.py` with known market shock data (e.g., 2020 crash). CVaR must capture the fat-tail average loss correctly.

## 🟢 Task 4: PortfolioAnalyzer & SLSQP Integration
Integrate MLX outputs with SciPy's constrained optimization.

- **Action**: Create `backend/utils/portfolio_analyzer.py`. Implement `optimize_weights` using `scipy.optimize.minimize` (SLSQP).
- **Action**: Enforce hard **10% Position Cap** and incorporate the **75 bps + Friction** dynamic hurdle in the objective function.
- **Action**: Add logic to toggle between **Risk-Budgeting** (Min Vol) and **Max Sharpe** personas.
- **Verification**: `tests/backend/test_portfolio_optimization.py`. Optimizer must never return a weight > 0.10.

## 🟢 Task 5: QuantEngine & Agent Integration
Expose optimization capabilities to the agentic layer.

- **Action**: Add `optimize_portfolio` method to `backend/quant_engine.py` as the main entry point.
- **Action**: Update `backend/agents/portfolio_agent.py` (or coordinator) to utilize the new optimization logic and format CVaR-backed responses with uncertainty intervals.
- **Verification**: Integrated test calling `QuantEngine.optimize_portfolio` with a sample portfolio.

## 🟢 Task 6: Asynchronous Optimization Monitor
Implement the background service for intraday rebalance triggers.

- **Action**: Create `backend/workers/optimization_monitor.py` using **ARQ**.
- **Action**: Implement the loop logic: Check drift (11.5% trigger) -> Run Optimizer -> If Alpha > Hurdle -> Push Agentic Alert.
- **Verification**: Start ARQ worker and mock a 12% drift event. Verify alert is generated in logs/DB.

## 🟢 Task 7: Frontend Persona & Alert Hook
Update SwiftUI dashboard to support the new optimization features.

- **Action**: Add "Strategy Persona" toggle (Defensive/Aggressive) to the portfolio view.
- **Action**: Update `ChatView` and `PassiveIncomeDashboard` to render CVaR risk metrics and rebalance suggestions with the established "NPU Glow" styling.
- **Verification**: Manual UI audit on M4 Mac.
