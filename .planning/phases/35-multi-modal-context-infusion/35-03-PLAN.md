---
phase: 35-multi-modal-context-infusion
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: [backend/agents/coordinator_agent.py, backend/agents/decision_agent.py, tests/vision/test_patterns.py]
autonomous: true
requirements: [VLM-03]

must_haves:
  truths:
    - "The final trade recommendation explicitly references visual evidence to support its thesis"
  artifacts:
    - path: "tests/vision/test_patterns.py"
      provides: "Integration tests for pattern detection"
  key_links:
    - from: "backend/agents/decision_agent.py"
      to: "MarketContext.vision"
      via: "context.vision.patterns"
---

<objective>
Connect the VisionAgent to the Multi-Agent Swarm and update DecisionAgent reasoning.
Purpose: Use visual evidence to improve trade decision accuracy.
Output: Integrated reasoning loop where visual signals influence final decisions.
</objective>

<execution_context>
@/Users/sanketmane/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/35-multi-modal-context-infusion/RESEARCH.md
@.planning/phases/35-multi-modal-context-infusion/35-VALIDATION.md
@backend/agents/coordinator_agent.py
@backend/agents/decision_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Pattern Detection Integration Tests</name>
  <files>tests/vision/test_patterns.py</files>
  <action>
    Create `tests/vision/test_patterns.py` to verify accurate identification of Head & Shoulders and Flags using sample chart screenshots.
  </action>
  <verify>
    <automated>PYTHONPATH=. pytest tests/vision/test_patterns.py</automated>
  </verify>
  <done>Integration tests for visual patterns created and passing.</done>
</task>

<task type="auto">
  <name>Task 2: Update CoordinatorAgent</name>
  <files>backend/agents/coordinator_agent.py</files>
  <action>
    Update `CoordinatorAgent` to detect when a query requires visual analysis (e.g., "look at the chart") and spawn the `VisionAgent`.
    - Ensure screenshot data is passed correctly through the MCP/Frontend bridge.
  </action>
  <verify>
    <automated>python3 -c "from backend.agents.coordinator_agent import CoordinatorAgent; print('Success')"</automated>
  </verify>
  <done>CoordinatorAgent can now orchestrate VisionAgent tasks.</done>
</task>

<task type="auto">
  <name>Task 3: Update DecisionAgent Prompt and Logic</name>
  <files>backend/agents/decision_agent.py</files>
  <action>
    Modify `DecisionAgent._build_prompt` to inject `context.vision` summaries.
    - Instruct the LLM to prioritize high-confidence visual patterns as confirmation signals.
    - Ensure visual evidence is mentioned in the final structured output.
  </action>
  <verify>
    <automated>python3 -c "from backend.agents.decision_agent import DecisionAgent; print('Success')"</automated>
  </verify>
  <done>DecisionAgent now consumes visual signals for final trade synthesis.</done>
</task>

</tasks>

<verification>
Run end-to-end trace verification command from 35-VALIDATION.md.
</verification>

<success_criteria>
DecisionAgent produces a response that says "Visual Pattern Confirmed: [X]" when a valid chart screenshot is provided.
</success_criteria>

<output>
After completion, create `.planning/phases/35-multi-modal-context-infusion/35-03-SUMMARY.md`
</output>
