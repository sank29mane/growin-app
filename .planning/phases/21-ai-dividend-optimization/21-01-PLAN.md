---
phase: 21-ai-dividend-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/data_models.py
  - backend/dividend_bridge.py
  - tests/backend/test_dividend_bridge.py
autonomous: true
requirements: [DIV-01, DIV-02]
must_haves:
  truths:
    - Unified dividend schema supports both T212 and Alpaca data formats
    - Robust IQR Scaling is applied to sparse dividend data
    - SEMPO EASD properly separates periodic dividend cycles from high-frequency market noise
  artifacts:
    - path: backend/dividend_bridge.py
      provides: Data normalization and transformation for dividend forecasts
    - path: backend/data_models.py
      provides: Unified dividend schema definition
  key_links:
    - from: backend/dividend_bridge.py
      to: ibm-granite-ttm
      via: data formatting for TTM-R2
---

<objective>
Implement the Dividend Data Engine and TTM Bridge to standardize and preprocess dividend data for the optimization agent.

Purpose: To ensure sparse and irregular dividend data from multiple brokers (T212/Alpaca) is robustly scaled (IQR) and filtered (SEMPO EASD) for accurate TTM-R2 modeling.
Output: Unified schema definitions, a fully functional `dividend_bridge.py` pipeline, and integration tests.
</objective>

<context>
@.planning/phases/21-ai-dividend-optimization/RESEARCH.md
@.planning/phases/21-ai-dividend-optimization/CONTEXT.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define Unified Dividend Schema</name>
  <files>backend/data_models.py</files>
  <action>Add a `DividendData` model (Pydantic) that unifies Trading 212 and Alpaca dividend data fields (ex-dividend date, amount, frequency). Include fields for IQR-scaled metrics.</action>
  <verify>pytest tests/backend/ -k "test_data_models" (Create a quick mock test if needed)</verify>
  <done>Unified schema successfully parses mock data from both brokers.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Dividend Bridge with Scaling & SEMPO</name>
  <files>backend/dividend_bridge.py</files>
  <action>Implement the `DividendBridge` class. Add a `robust_iqr_scale` method: `f_t = (f_t - median) / IQR`. Add a `sempo_easd_filter` method using `scipy.signal` to isolate periodic dividend cycles from market noise.</action>
  <verify>python -c "from backend.dividend_bridge import DividendBridge"</verify>
  <done>Bridge accurately scales and filters sample arrays without throwing exceptions on sparse data.</done>
</task>

<task type="auto">
  <name>Task 3: Integration Tests with Mock Dividend History</name>
  <files>tests/backend/test_dividend_bridge.py</files>
  <action>Write pytest test cases using a mock dividend history. Verify that the bridge handles <512 context points properly (fallback mechanism logic stubbing).</action>
  <verify>pytest tests/backend/test_dividend_bridge.py</verify>
  <done>All mock history tests pass, validating scaling and filtering logic.</done>
</task>
</tasks>
