---
phase: 21-ai-dividend-optimization
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - backend/agents/dividend_agent.py
  - backend/models/neural_ode.py
autonomous: true
requirements: [DIV-03, DIV-04, DIV-05]
must_haves:
  truths:
    - Agent enforces Consensus-Bottleneck (CB-APM) across TTM-R2, XGBoost, and Monte Carlo
    - Post-dividend recovery velocity is predicted using Neural ODEs
    - Hybrid order strategy transitions from Limit to Market within 2 hours of ex-div
    - Abort triggers fire if Drawdown > Dividend+5% or Confidence < 40%
  artifacts:
    - path: backend/agents/dividend_agent.py
      provides: The core optimization and risk evaluation logic
    - path: backend/models/neural_ode.py
      provides: Continuous-time recovery velocity modeling
  key_links:
    - from: backend/agents/dividend_agent.py
      to: backend/dividend_bridge.py
      via: preprocessed data consumption
---

<objective>
Build the core `DividendOptimizationAgent` enforcing consensus modeling and dynamic execution strategies.

Purpose: To synthesize seasonal trends, technical momentum, and continuous price recovery models, ensuring highly profitable and risk-averse dividend captures.
Output: The `DividendOptimizationAgent` module containing consensus routing, neural ODE integration, and hybrid order execution logic.
</objective>

<context>
@.planning/phases/21-ai-dividend-optimization/RESEARCH.md
@.planning/phases/21-ai-dividend-optimization/CONTEXT.md
@backend/dividend_bridge.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement Neural ODE Recovery Model</name>
  <files>backend/models/neural_ode.py</files>
  <action>Implement a low-rank Neural ODE using PyTorch/torchdyn to model `dh/dt = f(h(t), t, theta)` for predicting 'Recovery Velocity' post-dividend.</action>
  <verify>python -c "from backend.models.neural_ode import RecoveryODE"</verify>
  <done>Model initializes and can process a dummy forward pass of time-series tensors.</done>
</task>

<task type="auto">
  <name>Task 2: Build DividendOptimizationAgent & Consensus</name>
  <files>backend/agents/dividend_agent.py</files>
  <action>Create `DividendOptimizationAgent`. Implement the CB-APM consensus function routing TTM-R2, XGBoost, and Monte Carlo probabilities. Add "Gauge & Abort" logic: return abort signal if confidence <40% or max drawdown >5% over dividend.</action>
  <verify>python -c "from backend.agents.dividend_agent import DividendOptimizationAgent"</verify>
  <done>Consensus function correctly weights and routes signals, emitting correct abort triggers.</done>
</task>

<task type="auto">
  <name>Task 3: Hybrid Order Strategy Logic</name>
  <files>backend/agents/dividend_agent.py</files>
  <action>Add logic for Phase A (Limit Orders 2-3 days prior) and Phase B (Market Orders <2 hours before ex-dividend cutoff). Include the profit-taking lock mechanism to avoid intraday churn.</action>
  <verify>pytest tests/backend/ -k "test_dividend_agent"</verify>
  <done>Agent logic accurately shifts order types based on mock timestamp distances to ex-div date.</done>
</task>
</tasks>
