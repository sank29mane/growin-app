---
phase: 40-advanced-strategy-calibration
plan: 40-02
type: execute
wave: 2
depends_on: [40-01]
files_modified: [Growin/Views/CommandCenter/AlphaCommandDashboard.swift, Growin/Views/CommandCenter/AlphaLedgerView.swift]
autonomous: true
requirements: [SOV-02]

must_haves:
  truths:
    - "Alpha tracking chart maintains 120Hz smooth performance"
    - "Asset list implements the Brutalist Technical Ledger layout (No dividers)"
    - "Dashboard follows the radical asymmetric editorial grid"
  artifacts:
    - path: "Growin/Views/CommandCenter/AlphaCommandDashboard.swift"
      provides: "The primary 120Hz Alpha tracking dashboard shell"
    - path: "Growin/Views/CommandCenter/AlphaLedgerView.swift"
      provides: "Technical ledger representation of assets"
  key_links:
    - from: "Growin/Views/CommandCenter/AlphaCommandDashboard.swift"
      to: "SwiftUI.Canvas"
      via: "DrawingGroup or Canvas immediate-mode rendering"
---

<objective>
Implement the primary Alpha Command Dashboard using the Sovereign Foundation building blocks. This plan focuses on high-fidelity 120Hz performance for the Alpha-Stream tracker and a brutalist archival ledger for asset representation.

Purpose: Provide a high-authority interface for real-time strategy monitoring.
Output: 120Hz Alpha Dashboard and Technical Ledger view.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-advanced-strategy-calibration/40-RESEARCH.md
@.planning/phases/40-advanced-strategy-calibration/40-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement 120Hz Alpha-Stream Tracker (Canvas)</name>
  <files>Growin/Views/CommandCenter/AlphaCommandDashboard.swift</files>
  <skill>performance-optimizer</skill>
  <delegate>@ag-performance-optimizer</delegate>
  <action>
    Create `AlphaCommandDashboard.swift` implementing the Alpha chart using `SwiftUI.Canvas` for optimal 120Hz performance.
    - Reference Pattern 2 in 40-RESEARCH.md.
    - Use `Canvas` to draw streaming Alpha data (Reward over time) instead of individual View allocation.
    - Wrap the canvas in `.drawingGroup()` to offload rendering to Metal.
    - Use `.equatable()` on data containers to skip redrawing if data hasn't shifted significantly.
    - Implement the "Sovereign UI Shell" using the primitives from 40-01.
  </action>
  <verify>
    <automated>Verify that AlphaCommandDashboard uses 'Canvas' or '.drawingGroup()' for rendering performance</automated>
  </verify>
  <done>The Alpha tracker achieves a steady 120Hz frame rate on M4 Pro for real-time streams.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Brutalist Technical Ledger Asset List</name>
  <files>Growin/Views/CommandCenter/AlphaLedgerView.swift</files>
  <skill>frontend-design</skill>
  <delegate>@ag-frontend-specialist</delegate>
  <action>
    Implement `AlphaLedgerView.swift` for listing assets and current status.
    - Style: No dividers, tonal shifts (Recessed Charcoal for base, slightly lighter Charcoal for active rows).
    - Grid: Radical asymmetry, using technical typography (`Space Grotesk`) for numeric values and `Noto Serif` for ticker names.
    - Accents: Use `brutalChartreuse` only for critical alpha-positive signals.
    - Reference Pattern 1 (Tonal Layering) in 40-RESEARCH.md.
  </action>
  <verify>
    <automated>Verify AlphaLedgerView contains no 'Divider()' elements; uses Background tonal shifts instead</automated>
  </verify>
  <done>Asset lists follow the "Authority through Absence" mandate with a bespoke technical ledger feel.</done>
</task>

</tasks>

<verification>
- Verify 120Hz frame budget (8.33ms) using Xcode Instruments 'SwiftUI' template if possible.
- Visual check: No dividers or rounded corners in the ledger.
</verification>

<success_criteria>
- AlphaCommandDashboard renders at 120fps on ProMotion displays.
- Asset list uses tonal layering instead of standard iOS/macOS dividers.
- Visual integration with SovereignPrimitives is seamless.
</success_criteria>

<output>
After completion, create `.planning/phases/40-advanced-strategy-calibration/40-02-SUMMARY.md`
</output>
