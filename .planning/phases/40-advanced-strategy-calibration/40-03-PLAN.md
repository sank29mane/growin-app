---
phase: 40-advanced-strategy-calibration
plan: 40-03
type: execute
wave: 3
depends_on: [40-02]
files_modified: [Growin/Views/CommandCenter/AgentReasoningView.swift, Growin/Views/Calibration/StrategyLabView.swift, Growin/Views/ContentView.swift]
autonomous: true
requirements: [SOV-03]

must_haves:
  truths:
    - "Agent Reasoning Console renders logic traces in a high-authority archival ledger style"
    - "Calibration Lab provides visual feedback for PPO hyperparameter tuning"
    - "Main app entry point (ContentView) is updated to the Sovereign Ledger aesthetic"
  artifacts:
    - path: "Growin/Views/CommandCenter/AgentReasoningView.swift"
      provides: "The archival financial ledger-style reasoning trace"
    - path: "Growin/Views/Calibration/StrategyLabView.swift"
      provides: "Hyperparameter calibration UI for PPO tuning"
  key_links:
    - from: "Growin/Views/ContentView.swift"
      to: "Growin/Views/CommandCenter/AlphaCommandDashboard.swift"
      via: "Main navigation shell"
---

<objective>
Implement the final components of the Sovereign Alpha Command Center, focusing on agent reasoning transparency and strategy calibration. This plan completes the visual transformation by integrating the Reasoning Console and Strategy Lab into the core navigation.

Purpose: Provide deep visibility into strategy calibration and agent logic.
Output: Agent Reasoning Console, Strategy Lab View, and updated main app shell.
</objective>

<execution_context>
@~/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-advanced-strategy-calibration/40-RESEARCH.md
@.planning/phases/40-advanced-strategy-calibration/40-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Agent Reasoning Console (Archival Ledger Style)</name>
  <files>Growin/Views/CommandCenter/AgentReasoningView.swift</files>
  <skill>frontend-design</skill>
  <delegate>@ag-frontend-specialist</delegate>
  <action>
    Implement `AgentReasoningView.swift` to display the agent's thought traces.
    - Style: Archival financial ledger feel with high-contrast typography.
    - Typography: Use `Monaco` for the technical trace content and `Noto Serif` for section headers.
    - Layout: Use strictly vertical lines (1pt technical borders) to simulate a "columnar ledger" without using standard borders on rows.
    - Integration: Ensure it consumes the existing telemetry/trace stream from the backend.
  </action>
  <verify>
    <automated>Verify AgentReasoningView uses '.font(.custom("Monaco", size: ...))' for the trace body</automated>
  </verify>
  <done>Agent reasoning is presented with the "Brutal Editorial" authority, enhancing the transparency of autonomous decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Strategy Calibration Lab UI</name>
  <files>Growin/Views/Calibration/StrategyLabView.swift</files>
  <skill>frontend-design</skill>
  <delegate>@ag-frontend-specialist</delegate>
  <action>
    Create `StrategyLabView.swift` for PPO hyperparameter tuning.
    - Provide visual sliders and status indicators for hyperparameters (Learning Rate, Gamma, GAE-lambda, Clipping).
    - Use `SovereignPrimitives` for controls to ensure 0px corner consistency.
    - Implement a "Tuning Graph" that reflects the reward impact of parameter shifts (use `Canvas` or `Swift Charts` as appropriate).
    - Design should be "Authority through Absence" with minimal decoration.
  </action>
  <verify>
    <automated>Verify StrategyLabView uses SovereignButton and SovereignContainer primitives</automated>
  </verify>
  <done>Strategy Calibration Lab provides a high-fidelity interface for strategy tuning.</done>
</task>

<task type="auto">
  <name>Task 3: Final Integration and Main Shell Update</name>
  <files>Growin/Views/ContentView.swift</files>
  <skill>frontend-design</skill>
  <delegate>@ag-frontend-specialist</delegate>
  <action>
    Update `ContentView.swift` (or the root navigation view) to use the new `AlphaCommandDashboard` and `Sovereign Ledger` styling as the default experience.
    - Remove legacy "Liquid Glass" backgrounds/shadows from the root shell.
    - Set the global background color to `brutalRecessed`.
    - Wire navigation to include the Alpha Dashboard, Reasoning Console, and Calibration Lab.
  </action>
  <verify>
    <automated>Check if ContentView.swift references AlphaCommandDashboard and uses Sovereign Ledger style elements</automated>
  </verify>
  <done>The Growin App experience is fully transitioned to the Sovereign Alpha Command Center aesthetic.</done>
</task>

</tasks>

<verification>
- Run full visual regression: Ensure no legacy "Liquid Glass" elements (rounded corners, soft shadows) remain in the main user path.
- Final performance check: Confirm 120Hz stability across the integrated dashboard.
</verification>

<success_criteria>
- All views in the main navigation path follow the Sovereign Ledger style.
- Agent Reasoning Console is functional and styled with archival ledger precision.
- Strategy Lab allows for hyperparameter visualization.
- Overall app performance remains fluid on ProMotion displays.
</success_criteria>

<output>
After completion, create `.planning/phases/40-advanced-strategy-calibration/40-03-SUMMARY.md`
</output>
