diff --git a/backend/agents/coordinator_agent.py b/backend/agents/coordinator_agent.py
index 299c2eb..aad6398 100644
--- a/backend/agents/coordinator_agent.py
+++ b/backend/agents/coordinator_agent.py
@@ -374,12 +374,12 @@ class CoordinatorAgent(BaseAgent):
         The script will run in an ISOLATED Docker container.
         
         Input Context:
-        {json.dumps(context)}
+        {json.dumps(context, default=str)}
         
         Return ONLY a JSON block with the script:
         {{
           "reasoning": "Explain the fix",
-          "code": "import json\\ncontext = {json.dumps(context)}\\n# ... fix logic ...\\nprint(json.dumps(context))"
+          "code": "import json\\ncontext = {json.dumps(context, default=str)}\\n# ... fix logic ...\\nprint(json.dumps(context, default=str))"
         }}
         """
         try:
diff --git a/backend/utils/worker_service.py b/backend/utils/worker_service.py
index f16b09b..f0cab28 100644
--- a/backend/utils/worker_service.py
+++ b/backend/utils/worker_service.py
@@ -239,7 +239,7 @@ def main():
                     return [sanitize(v) for v in obj]
                 return obj
             
-            sys.stdout.write(json.dumps(sanitize(response)) + "\n")
+            sys.stdout.write(json.dumps(sanitize(response), default=str) + "\n")
             sys.stdout.flush()
         except json.JSONDecodeError:
             logger.error("Failed to decode JSON request")
diff --git a/future/suggestions.MD b/future/suggestions.MD
index 7d7b7bd..55ea069 100644
--- a/future/suggestions.MD
+++ b/future/suggestions.MD
@@ -12,3 +12,4 @@ This document tracks visionary or architectural suggestions identified during co
 | 004 | Strategy Suggestion Engine | Enhance the Coordinator Agent to provide proactive "Strategic Suggestions" (e.g., diversification tips, tax-loss harvesting alerts) during routine portfolio analysis. | Current analysis is reactive. Proactive suggestions increase the "Alpha" value of the agentic swarm. | Proposed |
 | 005 | Quant Engine Standardization | `analyze_rebalancing_opportunity` assumes target allocations are given as valid fractions (e.g. 0.5) when users often provide percentages. | Standardize fractional representations inside `analyze_rebalancing_opportunity` to properly divide allocations > 1.0. | Completed |
 | 006 | Centralized MLX Conditional Import | Abstract MLX imports to a single centralized utility module (`backend/utils/mlx_loader.py`). | Widespread `try...except ImportError` blocks with mocking logic for `mlx.core` and `mlx.nn` are duplicated across multiple agents (`rl_policy`, `ppo_agent`, `rl_utils`). A central module would simplify test execution across non-Apple Silicon CI environments and keep business logic clean. | Completed |
+| 007 | Agent API Standardization | Standardize the `analyze` and `execute` method signatures and internal handling across all classes inheriting from `BaseAgent`. | Some agents (e.g. `PortfolioAgent`) mix internal sync analysis functions (`analyze_portfolio_quality`) with async class methods (`analyze(self, context)`), while others do not implement `analyze` identically. Standardization reduces routing complexities and simplifies testing. | Proposed |

